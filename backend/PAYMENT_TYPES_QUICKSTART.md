# 🚀 ملخص: كيفية إضافة وسيلة دفع جديدة

## ✅ تم تنفيذ النظام الديناميكي بنجاح!

### 📊 الوضع الحالي
- ✅ **11 نوع وسيلة دفع** جاهزة في القاعدة
- ✅ **API Endpoint** يعمل: `/api/payment-types`
- ✅ **Backend** يدعم الإضافة/الحذف ديناميكياً

---

## 🎯 لإضافة وسيلة دفع جديدة (مثال: Binance Pay)

### الطريقة 1: عبر API ⚡ (الأسرع)

```bash
curl -X POST http://localhost:8001/api/payment-types \
  -H "Content-Type: application/json" \
  -d '{
    "code": "binance_pay",
    "name_ar": "Binance Pay",
    "name_en": "Binance Pay",  
    "icon": "₿",
    "category": "crypto",
    "sort_order": 12
  }'
```

### الطريقة 2: عبر قاعدة البيانات

```bash
cd backend
sqlite3 app.db "INSERT INTO payment_type (code, name_ar, name_en, icon, category, is_active, sort_order) VALUES ('binance_pay', 'Binance Pay', 'Binance Pay', '₿', 'crypto', 1, 12);"
```

### الطريقة 3: عبر واجهة المستخدم (مستقبلاً)
سنضيف شاشة "إدارة أنواع وسائل الدفع" في الإعدادات

---

## 📝 الحقول المطلوبة

| الحقل | الوصف | مثال |
|-------|-------|------|
| `code` | كود فريد (إنجليزي، بدون مسافات) | `binance_pay` |
| `name_ar` | الاسم بالعربية | `Binance Pay` |
| `name_en` | الاسم بالإنجليزية (اختياري) | `Binance Pay` |
| `icon` | أيقونة إيموجي | `₿` |
| `category` | التصنيف | `crypto` |
| `sort_order` | الترتيب (رقم) | `12` |

---

## 🏷️ التصنيفات المتاحة

- `card` - بطاقات بنكية (مدى، فيزا، ماستركارد)
- `mobile_wallet` - محافظ إلكترونية (STC Pay، Apple Pay، UrPay)
- `bnpl` - اشتر الآن وادفع لاحقاً (تمارا، تابي)
- `cash` - نقد
- `crypto` - عملات رقمية
- `bank_transfer` - تحويل بنكي

---

## 🔄 الخطوات الكاملة (مثال عملي)

### 1. إضافة النوع
```bash
curl -X POST http://localhost:8001/api/payment-types \
  -H "Content-Type: application/json" \
  -d '{"code": "urpay", "name_ar": "يور باي", "icon": "📱", "category": "mobile_wallet"}'
```

### 2. إضافة الحساب المحاسبي
- افتح "الدليل المحاسبي"
- أضف حساب جديد:
  - **رقم**: `1160`
  - **الاسم**: `محفظة UrPay`
  - **النوع**: `receivable` (أوراق قبض)
  - **الأصل**: `100` (النقدية وما في حكمها)

### 3. إضافة وسيلة الدفع
- افتح "وسائل الدفع"
- اضغط "إضافة وسيلة دفع"
- **النوع**: اختر "يور باي 📱" (ستظهر تلقائياً!)
- **الحساب**: اختر "1160 - محفظة UrPay"
- **العمولة**: `2.5%`
- احفظ

**النتيجة:**
- ✅ يُنشأ حساب تلقائياً: `1160.1 - UrPay`
- ✅ جاهز للاستخدام في الفواتير فوراً!

---

## 📊 الأنواع الحالية (11 نوع)

### البطاقات البنكية 💳
1. مدى (`mada`)
2. فيزا (`visa`)  
3. ماستركارد (`mastercard`)
4. أمريكان إكسبريس (`amex`)

### المحافظ الإلكترونية 📱
5. Apple Pay (`apple_pay`)
6. STC Pay (`stc_pay`)
7. يور باي (`urpay`)

### اشتر الآن وادفع لاحقاً 🛍️
8. تمارا (`tamara`)
9. تابي (`tabby`)

### أخرى
10. نقداً 💵 (`cash`)
11. عملات رقمية ₿ (`crypto`)

---

## 🔍 التحقق من الأنواع المتاحة

```bash
# عرض جميع الأنواع
curl http://localhost:8001/api/payment-types

# عرض نوع محدد
curl http://localhost:8001/api/payment-types | jq '.[] | select(.code=="urpay")'
```

---

## ❌ حذف نوع وسيلة دفع

```bash
# احصل على ID النوع المراد حذفه
curl http://localhost:8001/api/payment-types | jq '.[] | select(.code=="crypto") | .id'

# احذف (مثال: ID = 11)
curl -X DELETE http://localhost:8001/api/payment-types/11
```

**ملاحظة:** لا يمكن حذف نوع مستخدم في وسائل دفع نشطة ⚠️

---

## 🎯 أمثلة وسائل دفع مستقبلية

يمكنك إضافة:
- **Stripe**: `{"code": "stripe", "name_ar": "Stripe", "icon": "💰", "category": "card"}`
- **PayPal**: `{"code": "paypal", "name_ar": "باي بال", "icon": "🅿️", "category": "mobile_wallet"}`
- **Klarna**: `{"code": "klarna", "name_ar": "كلارنا", "icon": "🛒", "category": "bnpl"}`
- **تحويل بنكي**: `{"code": "bank_transfer", "name_ar": "تحويل بنكي", "icon": "🏦", "category": "bank_transfer"}`
- **شيك**: `{"code": "cheque", "name_ar": "شيك", "icon": "📋", "category": "cheque"}`

---

## ✨ المزايا

✅ **ديناميكي 100%** - لا حاجة لتعديل الكود
✅ **متعدد اللغات** - عربي + إنجليزي
✅ **مصنف ومنظم** - categories واضحة
✅ **آمن** - لا يمكن حذف نوع مستخدم
✅ **قابل للتوسع** - إضافة لا محدودة

---

## 📂 الملفات ذات الصلة

- **Backend Model**: `/backend/models.py` (PaymentType class)
- **Backend Routes**: `/backend/payment_methods_routes.py`
- **Seed Data**: `/backend/seed_payment_types.py`
- **Guide**: `/backend/PAYMENT_TYPES_GUIDE.md`

---

## 🚀 الخطوة التالية

لتفعيل النظام في Flutter:
1. تعديل `payment_methods_screen.dart`
2. استبدال القائمة الثابتة بـ `_fetchPaymentTypes()` من API
3. الآن أي إضافة جديدة تظهر فوراً في التطبيق! 🎉

**راجع ملف `PAYMENT_TYPES_GUIDE.md` للتفاصيل الكاملة**
