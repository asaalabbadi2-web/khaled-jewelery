# ⚠️ ملاحظة هامة جداً - IMPORTANT

## 🔴 يجب تفعيل البيئة الافتراضية أولاً!

**قبل تشغيل أي ملف Python في هذا المجلد، يجب تفعيل البيئة الافتراضية:**

```bash
# الخطوة 1: الانتقال للمجلد
cd /Users/salehalabbadi/yasargold/backend

# الخطوة 2: تفعيل البيئة الافتراضية (إلزامي!)
source venv/bin/activate

# الخطوة 3: الآن يمكنك تشغيل أي أمر Python
python app.py
python test_invoices.py
alembic upgrade head
# ... إلخ
```

**علامة التفعيل:** ستظهر `(venv)` في بداية سطر الأوامر عند التفعيل الناجح.

---

## ملاحظة هامة

### تعليمات إدخال البيانات بالوزن

- جميع الحقول الأساسية (الوزن، العيار، الأجرة) يجب إدخالها بوحدة الوزن (غرام) وليس بالنقدية.
- عند إدخال صنف جديد أو فاتورة، تأكد من إدخال الوزن الفعلي للذهب، وسيتم تحويله تلقائياً إلى العيار الرئيسي.
- الأجرة والمصاريف يتم تحويلها إلى ما يعادلها بالذهب تلقائياً.
- التقارير والجرد تعرض حركة الذهب والربح والخسارة بالوزن.

---

## أدوات مساعدة

### فحص إعدادات الحسابات المحاسبية

السكريبت `check_mappings.py` يعرض تقرير شامل عن إعدادات الحسابات المحاسبية:

```bash
# تفعيل البيئة الافتراضية أولاً
source venv/bin/activate

# تشغيل الفحص
python check_mappings.py
```

**يعرض التقرير:**
- جميع الإعدادات الحالية مجمعة حسب نوع العملية
- الحسابات الأساسية المتوفرة
- الإعدادات الناقصة لكل نوع عملية

**لإضافة إعدادات جديدة:**
استخدم API بدلاً من السكريبت (انظر `docs/accounting_mapping.md`)

---