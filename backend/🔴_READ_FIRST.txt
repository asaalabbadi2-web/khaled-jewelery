
╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║   🔴 STOP! READ THIS BEFORE RUNNING ANY PYTHON COMMAND 🔴    ║
║                                                               ║
║   تنبيه! اقرأ هذا قبل تشغيل أي أمر Python                    ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│                                                               │
│  ⚠️  يجب تفعيل البيئة الافتراضية أولاً!                     │
│  ⚠️  Must activate virtual environment first!                │
│                                                               │
└───────────────────────────────────────────────────────────────┘

الخطوات الصحيحة / Correct Steps:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  الانتقال للمجلد:
    cd /Users/salehalabbadi/yasargold/backend

2️⃣  تفعيل البيئة الافتراضية (إلزامي!):
    source venv/bin/activate

3️⃣  تأكد من ظهور (venv) في بداية السطر:
    (venv) salehalabbadi@Mac backend %  ← ✅ صحيح

4️⃣  الآن يمكنك تشغيل أي أمر Python:
    python app.py
    python test_invoices.py
    alembic upgrade head
    pip install -r requirements.txt


❌ خطأ شائع / Common Mistake:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DON'T DO THIS:
    cd backend
    python app.py  ← ❌ خطأ! لم يتم تفعيل venv

DO THIS INSTEAD:
    cd backend
    source venv/bin/activate  ← ✅
    python app.py            ← ✅


📌 لماذا هذا مهم؟ / Why is this important?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ تضمن استخدام المكتبات الصحيحة
  Ensures correct library versions

✓ تتجنب مشاكل التوافق
  Avoids compatibility issues

✓ تعمل البيئة بشكل صحيح
  Environment works correctly

✓ لا مشاكل في الـ imports
  No import errors


💡 نصيحة / Tip:
━━━━━━━━━━━━━━━━

يمكنك إنشاء alias في ~/.zshrc:

    alias yasarcd='cd /Users/salehalabbadi/yasargold/backend && source venv/bin/activate'

ثم استخدام:
    yasarcd
    python app.py


📞 للمساعدة / For Help:
━━━━━━━━━━━━━━━━━━━━━━━━

إذا واجهت مشاكل:
1. تأكد من وجود مجلد venv/
2. إذا لم يكن موجوداً، أنشئه:
   python3 -m venv venv
3. ثم فعّله:
   source venv/bin/activate


╔═══════════════════════════════════════════════════════════════╗
║  تذكر: دائماً (venv) أولاً! / Remember: Always (venv) first! ║
╚═══════════════════════════════════════════════════════════════╝
