name: Build & Push Docker Images

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute GHCR repo (lowercase)
        id: vars
        shell: bash
        run: |
          echo "ghcr_repo=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}

      - name: Build & push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.vars.outputs.ghcr_repo }}/backend:latest
            ghcr.io/${{ steps.vars.outputs.ghcr_repo }}/backend:${{ github.sha }}

      - name: Build & push web (nginx+flutter) image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/Dockerfile
          push: true
          build-args: |
            API_BASE_URL=/api
          tags: |
            ghcr.io/${{ steps.vars.outputs.ghcr_repo }}/web:latest
            ghcr.io/${{ steps.vars.outputs.ghcr_repo }}/web:${{ github.sha }}
