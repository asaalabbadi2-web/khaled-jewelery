# تقرير تاريخ أسعار الذهب - الإصدار الأول

## الملفات المعدّلة
- `backend/routes.py` → واجهة `/reports/gold_price_history` مع التجميع (يومي/أسبوعي/شهري) وإحصاءات التقلّب والتحويل للريال والعيار الرئيسي
- `frontend/lib/api_service.dart` → إضافة `getGoldPriceHistoryReport` مع معاملات المدى الزمني، فترة التجميع، وحد النتائج
- `frontend/lib/screens/reports/gold_price_history_report_screen.dart` → شاشة تفاعلية تعرض ملخصات، آخر سعر، رسم خطي، وجدول تفصيلي للفترات
- `frontend/lib/models/report_catalog.dart` + `frontend/lib/screens/reports/reports_main_screen.dart` → تفعيل التقرير داخل مركز التقارير والتنقل إليه
- `frontend/lib/models/quick_action_item.dart` + `frontend/lib/screens/home_screen_enhanced.dart` → زر وصول سريع جديد "تقرير سعر الذهب" من الشاشة الرئيسية

## أبرز القدرات
1. **لوحة ملخص حرارية**
  - متوسط السعر بالدولار والريال (عيار 24 والعيار الرئيسي)
  - تغير مطلق ونسبته المئوية مع قراءة مباشرة لتقلب الأسعار (Standard Deviation)

2. **بطاقة آخر سعر**
  - يعرض سعر الأونصة بالدولار، وسعر الجرام (عيار 24 والعيار الرئيسي) مع ختم التوقيت المحلي
  - تصميم ذهبي متناغم مع هوية النظام

3. **مخطط خطي سلس باستخدام `fl_chart`**
  - يظهر مسار متوسط السعر بالدولار عبر الفترات المختارة مع تدرج ذهبي وتظليل أسفل المنحنى
  - محاور مهيأة تلقائياً مع وسم كل رابع نقطة لقراءة مريحة

4. **جدول تفصيلي للفترات**
  - يعرض متوسط السعر بالدولار والريال، أعلى/أدنى قراءة، التغير النسبي، وشارة الاتجاه (تصاعدي/متراجع/مستقر)

5. **خيارات فلترة مرنة**
  - اختيار مدى التاريخ، مسح التحديد بسرعة، تغيير فترة التجميع (يومي/أسبوعي/شهري)، وتحديد حد النقاط (30 حتى 730)
  - الواجهة تعمل باللغتين وتدعم السحب للتحديث

> للوصول: مركز التقارير ➜ قسم تقارير الذهب ➜ "تاريخ أسعار الذهب" أو عبر عنصر الوصول السريع "تقرير سعر الذهب".

---

# تقرير الأصناف منخفضة المخزون - الإصدار الأول

## الملفات المعدّلة
- `backend/routes.py` → إضافة واجهة `/reports/low_stock` بتحويلات الوزن والعتبات المرنة
- `frontend/lib/api_service.dart` → دالة `getLowStockReport` مع جميع معاملات الفرز والفلترة
- `frontend/lib/screens/reports/low_stock_report_screen.dart` → شاشة Flutter كاملة ببطاقات التلخيص، مخطط الأعمدة، وجدول تفصيلي
- `frontend/lib/models/report_catalog.dart` + `frontend/lib/screens/reports/reports_main_screen.dart` → إتاحة التقرير الجديد داخل مركز التقارير والتنقل إليه
- `docs/reports_roadmap.md` → تحديث حالة تقرير Low Stock إلى ✅ مع وصف القدرات النهائية

## أبرز القدرات
1. **خيارات فلترة متقدمة**
  - شرائح عيار متعددة، معرّف مكتب اختياري، حدود نقص للكمية/الوزن، وحدود أعلى النتائج
  - تبديل فوري لخيارات تضمين المخزون الصفري والقيود غير المرحلة، بالإضافة للتحكم باتجاه الفرز

2. **لوحات ملخص ذكية**
  - عرض عدد الأصناف المتأثرة، الحالات الحرجة، إجمالي نقص الكمية والوزن، ومتوسط الأيام دون حركة
  - شارات معلومات توضح عدد الأصناف المحلّلة ووقت توليد التقرير

3. **مخطط أخطر الأصناف**
  - رسم أعمدة باستخدام `fl_chart` يبرز أعلى 8 أصناف من حيث وزن العجز
  - وسوم توضيحية تعرض الاسم ونقص الوزن، مع ألوان ذهبية متوافقة مع هوية النظام

4. **جدول تفاعلي للمخزون المنخفض**
  - جدول قابل للتمرير أفقيًا مع أعمدة للكمية والوزن المتاحين، نقص المخزون، الحالة، وأيام عدم الحركة
  - شارات حالة ملونة (حرج/منخفض/مستقر) لتسريع اتخاذ القرار

> للوصول إلى التقرير: مركز التقارير ➜ قسم تقارير المخزون ➜ "أصناف منخفضة المخزون".

---

# تقرير أعمار الذمم للعملاء - الإصدار الأول

## الملفات المعدّلة
- `backend/routes.py` → إضافة واجهة `/reports/customer_balances_aging`
- `frontend/lib/api_service.dart` → استدعاء `getCustomerBalancesAgingReport`
- `frontend/lib/screens/reports/customer_balances_aging_report_screen.dart` → شاشة Flutter جديدة بكروت التلخيص والرسوم والجداول
- `frontend/lib/models/report_catalog.dart` + `frontend/lib/screens/reports/reports_main_screen.dart` → إدراج التقرير داخل كتالوج مركز التقارير

## أبرز القدرات
1. **خيارات فلترة مرنة**
  - اختيار تاريخ الترحيل (Cutoff)
  - إظهار الأرصدة الصفرية أو إخفاؤها
  - تضمين القيود غير المرحلة عند الحاجة
  - تصفية حسب مجموعة العملاء عبر معرف المجموعة

2. **لوحات ملخص ذكية**
  - إجمالي الذمم نقداً وذهباً، والأرصدة الدائنة، وعدد العملاء
  - تنسيقات عملة ووزن مرتبطة بخيارات المستخدم داخل `SettingsProvider`

3. **مخطط توزيع الأعمار**
  - رسم أعمدة ثنائي (نقد/ذهب) يعتمد على `fl_chart`
  - يدعم المسميات العربية والإنجليزية تلقائياً

4. **قائمة أهم المتأخرين**
  - إبراز العملاء ذوي أرصدة 90+ يوم مع قيم نقدية واضحة

5. **جدول Drill-Down كامل**
  - يعرض كل عميل مع رصيد نقدي/ذهبي وتوزيع الأعمار ومتوسط التأخير
  - تصميم أفقي قابل للتمرير لتلاؤم الأعمدة العديدة

6. **تجربة مستخدم محسّنة**
  - دعم RTL كامل، رسائل خطأ باللغة المختارة، ومؤشرات تحميل/تحديث بالسحب

> لعرض التقرير الجديد انتقل إلى مركز التقارير ➜ قسم التقارير المالية ➜ "تقرير أعمار الذمم للعملاء".

---

# آخر التحديثات على شاشة الفاتورة

## الملف المعدل
`frontend/lib/screens/sales_invoice_screen_v2.dart`

## التعديلات المنفذة

### 1. إصلاح overflow في dropdown طرق الدفع (السطور 1718-1760)
**المشكلة:** عند وجود اسم طويل لطريقة الدفع، كان يحدث overflow

**الحل:**
- حذف badge العمولة المنفصل
- دمج نص العمولة داخل Column
- تقليل الأحجام:
  - حجم الأيقونة: `20` (كان `22`)
  - خط الاسم: `13` (كان `14`)
  - خط العمولة: `10` (كان `11`)
  - padding: `6` عمودي (كان `8`)
- إضافة `overflow: TextOverflow.ellipsis` و `maxLines: 1` للنصوص

```dart
// السطر 1740 تقريباً
Text(
  method['name'] ?? '',
  style: const TextStyle(fontSize: 13, ...),
  overflow: TextOverflow.ellipsis,
  maxLines: 1,
),
```

---

### 2. تحسين حقل إدخال المبلغ (السطور 1782-1866)
**التحسينات:**
- حدود زرقاء ديناميكية عند وجود مبلغ متبقي
- shadow خفيف للتركيز
- خط أكبر: `fontSize: 16` (كان `14`)
- وزن خط أقوى: `FontWeight.w600`
- إضافة `suffixText: 'ر.س'` داخل الحقل
- label بلون أزرق احترافي

```dart
// السطر 1806
decoration: InputDecoration(
  labelText: 'المبلغ',
  labelStyle: TextStyle(
    color: Colors.blue.shade700,
    fontWeight: FontWeight.w600,
    fontFamily: 'Cairo',
  ),
  suffixText: 'ر.س',
  ...
),
```

---

### 3. أيقونة ملء باقي المبلغ ⭐ **NEW**
**الميزة الجديدة:** (السطور 1837-1858)

زر أيقونة يظهر **فقط عندما يوجد مبلغ متبقي** لملء الحقل تلقائياً

```dart
if (_remainingAmount > 0)
  Container(
    decoration: BoxDecoration(
      border: Border(
        right: BorderSide(color: Colors.blue.shade200),
      ),
    ),
    child: IconButton(
      icon: Icon(
        Icons.playlist_add_check,
        color: Colors.blue.shade700,
        size: 24,
      ),
      tooltip: 'ملء باقي المبلغ (${_remainingAmount.toStringAsFixed(2)})',
      onPressed: () {
        setState(() {
          _customAmountController.text = _remainingAmount.toStringAsFixed(2);
        });
      },
    ),
  ),
```

**المزايا:**
- 🎯 ظهور ديناميكي حسب وجود متبقي
- 🔵 تصميم متناسق مع الحقل
- 💡 tooltip توضيحي بالمبلغ الدقيق
- ⚡ نقرة واحدة لملء المبلغ
- 🎨 فاصل بصري (border يمين)

---

### 4. تحسين شكل المبالغ في جدول الدفعات (السطور 1562-1600)
**التحسينات:**
- المبلغ الأصلي:
  - حجم: `16` (كان `14`)
  - لون: `Colors.green.shade800`
  - وحدة: `ر.س`
  - وزن: `FontWeight.w700`

- العمولة:
  - حجم: `15`
  - لون: `Colors.red.shade700`
  - عرض `-` عند عدم وجود عمولة
  - وحدة: `ر.س`

- الصافي:
  - حجم: `16`
  - لون: `Colors.blue.shade800`
  - وحدة: `ر.س`
  - وزن: `FontWeight.w700`

```dart
// السطر 1569 تقريباً
Text(
  '${payment.amount.toStringAsFixed(2)} ر.س',
  style: TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w700,
    color: Colors.green.shade800,
    fontFamily: 'Cairo',
  ),
),
```

---

## كيفية رؤية التغييرات

### طريقة 1: Hot Reload (إذا كان التطبيق يعمل)
1. اذهب لنافذة التطبيق في Chrome
2. اضغط `r` في Terminal للـ hot reload
3. أو أعد تحميل الصفحة

### طريقة 2: إعادة التشغيل
```bash
cd /Users/salehalabbadi/yasargold/frontend
flutter run -d chrome
```

---

## الملفات المستخدمة حالياً

التأكد من أن التطبيق يستخدم الملفات الصحيحة:

✅ `frontend/lib/main.dart` → يستورد `screens/home_screen.dart`
✅ `frontend/lib/screens/home_screen.dart` → يستورد `sales_invoice_screen_v2.dart`
✅ `frontend/lib/screens/sales_invoice_screen_v2.dart` → **الملف الذي تم تعديله**

---

## التحقق من التعديلات

لرؤية التعديلات بنفسك:

```bash
# الأيقونة الجديدة
grep -A 15 "Icons.playlist_add_check" frontend/lib/screens/sales_invoice_screen_v2.dart

# Dropdown المحسّن
grep -A 30 "fontSize: 13" frontend/lib/screens/sales_invoice_screen_v2.dart | head -35

# المبالغ المحسّنة
grep "fontSize: 16" frontend/lib/screens/sales_invoice_screen_v2.dart
```

---

## ملاحظات

- جميع التعديلات في `sales_invoice_screen_v2.dart`
- لا توجد تعديلات على Backend
- التعديلات متوافقة مع الكود الموجود
- لا تحتاج أي dependencies إضافية

تاريخ التحديث: 16 أكتوبر 2025
