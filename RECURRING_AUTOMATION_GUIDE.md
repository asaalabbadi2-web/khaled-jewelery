# 🤖 دليل الأتمتة: كيف يعمل النظام تلقائياً؟
## آلية التشغيل التلقائي للقيود الدورية

**التاريخ**: 8 نوفمبر 2025  
**الجمهور**: المسؤولون التقنيون والمطورون

---

## 📖 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [كيف يعمل النظام؟](#كيف-يعمل-النظام)
3. [إعداد Cron Job](#إعداد-cron-job)
4. [المراقبة والتحقق](#المراقبة-والتحقق)
5. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة

النظام يعمل على **3 مستويات**:

### 1️⃣ المستوى اليدوي (Manual)
```
المستخدم → زر "إنشاء الآن" → API → قيد جديد
```
- المستخدم يختار القالب ويضغط "إنشاء الآن"
- يُرسل طلب `POST /api/recurring_templates/{id}/create_entry`
- يُنشأ القيد فوراً

### 2️⃣ المستوى شبه التلقائي (Semi-Auto)
```
المدير → زر "معالجة الكل" → API → جميع القيود المستحقة
```
- المدير يضغط "معالجة جميع القيود المستحقة"
- يُرسل طلب `POST /api/recurring_templates/process_all`
- يُنشأ جميع القيود التي حان موعدها

### 3️⃣ المستوى التلقائي الكامل (Full Auto) ⭐
```
Cron → كل يوم 1:00 ص → Python Script → جميع القيود المستحقة
```
- يعمل تلقائياً بدون تدخل بشري
- يفحص القوالب النشطة يومياً
- ينشئ القيود في الوقت المحدد

---

## 🔄 كيف يعمل النظام؟

### المنطق الأساسي:

```python
# من ملف backend/process_recurring_journals.py

from recurring_journal_system import process_recurring_journals
from datetime import date

# 1. استدعاء الدالة الرئيسية
results = process_recurring_journals()

# 2. النتائج
print(f"✅ تم إنشاء {results['created']} قيود")
print(f"⏩ تم تخطي {results['skipped']} قوالب")
print(f"❌ فشل {results['failed']} محاولات")
```

### ما الذي يحدث داخل `process_recurring_journals()`؟

```python
# المنطق المبسط:

def process_recurring_journals():
    today = date.today()
    
    # 1. جلب القوالب النشطة فقط
    templates = RecurringJournalTemplate.query.filter_by(
        is_active=True,
        auto_create=True
    ).all()
    
    results = {'created': 0, 'skipped': 0, 'failed': 0}
    
    for template in templates:
        # 2. هل حان موعد هذا القالب؟
        if template.next_run_date <= today:
            try:
                # 3. إنشاء القيد
                entry = template.create_journal_entry()
                results['created'] += 1
                print(f"✅ تم إنشاء القيد {entry.entry_number}")
            except Exception as e:
                results['failed'] += 1
                print(f"❌ خطأ: {e}")
        else:
            results['skipped'] += 1
    
    return results
```

### مثال عملي:

**القالب:**
- الاسم: "راتب موظفي المحل"
- النوع: شهري (يوم 25)
- `next_run_date`: 2025-11-25
- `is_active`: True
- `auto_create`: True

**ما يحدث:**

| التاريخ | الفحص | الإجراء | النتيجة |
|---------|-------|---------|---------|
| 2025-11-24 | `2025-11-25 > 2025-11-24` | ⏩ تخطي | لم يحن الموعد بعد |
| 2025-11-25 | `2025-11-25 <= 2025-11-25` | ✅ إنشاء | قيد جديد JE-2025-00020 |
| 2025-11-26 | `2025-12-25 > 2025-11-26` | ⏩ تخطي | تم التحديث للشهر القادم |

**بعد الإنشاء:**
- `total_created`: 0 → 1
- `last_created_date`: 2025-11-25
- `next_run_date`: 2025-11-25 → 2025-12-25 (الشهر التالي)

---

## ⚙️ إعداد Cron Job

### الطريقة 1: استخدام السكريبت المساعد ✅

```bash
cd /Users/salehalabbadi/yasargold/backend
bash setup_cron.sh
```

**سيُظهر لك:**
```
🔧 إعداد Cron Job للقيود الدورية...

الأمر المقترح للإضافة:
0 1 * * * cd /Users/salehalabbadi/yasargold/backend && source venv/bin/activate && python process_recurring_journals.py >> /tmp/recurring_journals.log 2>&1

لإضافة Cron Job، قم بتشغيل:
  crontab -e

ثم أضف السطر التالي:
0 1 * * * cd /Users/salehalabbadi/yasargold/backend && source venv/bin/activate && python process_recurring_journals.py >> /tmp/recurring_journals.log 2>&1

💡 سيتم تشغيل المعالجة التلقائية يومياً الساعة 1:00 صباحاً
```

### الطريقة 2: يدوياً

**الخطوة 1: افتح crontab**
```bash
crontab -e
```

**الخطوة 2: أضف السطر التالي**
```bash
# القيود الدورية - تشغيل يومي الساعة 1:00 ص
0 1 * * * cd /Users/salehalabbadi/yasargold/backend && source venv/bin/activate && python process_recurring_journals.py >> /tmp/recurring_journals.log 2>&1
```

**الخطوة 3: احفظ واخرج**
- في `vim`: اضغط `ESC` ثم `:wq`
- في `nano`: اضغط `Ctrl+X` ثم `Y` ثم `Enter`

**الخطوة 4: تحقق من التثبيت**
```bash
crontab -l
```

يجب أن ترى السطر المضاف.

---

## 📊 شرح صيغة Cron

```
0 1 * * *
│ │ │ │ │
│ │ │ │ └─── يوم الأسبوع (0-7، 0 و 7 = الأحد)
│ │ │ └───── الشهر (1-12)
│ │ └─────── يوم الشهر (1-31)
│ └───────── الساعة (0-23)
└─────────── الدقيقة (0-59)
```

### أمثلة أخرى:

| الصيغة | الوقت | الوصف |
|--------|-------|-------|
| `0 1 * * *` | كل يوم 1:00 ص | (المستخدم حالياً) |
| `0 */2 * * *` | كل ساعتين | كل ساعة زوجية |
| `30 9 * * 1-5` | 9:30 ص من الإثنين-الجمعة | أيام العمل |
| `0 0 1 * *` | منتصف الليل، أول يوم من كل شهر | شهرياً |
| `0 2 * * 0` | 2:00 ص كل أحد | أسبوعياً |

### لتغيير الوقت:

**لتشغيل الساعة 2:00 صباحاً:**
```bash
0 2 * * * cd /path/to/backend && source venv/bin/activate && python process_recurring_journals.py >> /tmp/recurring_journals.log 2>&1
```

**لتشغيل كل 6 ساعات:**
```bash
0 */6 * * * cd /path/to/backend && source venv/bin/activate && python process_recurring_journals.py >> /tmp/recurring_journals.log 2>&1
```

---

## 📝 المراقبة والتحقق

### 1️⃣ فحص سجل التشغيل (Log File)

```bash
tail -f /tmp/recurring_journals.log
```

**مثال على المخرجات:**
```
==========================================
Recurring Journals Processing Started
Time: 2025-11-08 01:00:01
==========================================
✅ Created entry JE-2025-00023 from template 'راتب موظفي المحل'
⏩ Skipped template 'إيجار المحل' (next run: 2025-12-01)
⏩ Skipped template 'فواتير الخدمات' (next run: 2025-11-15)
==========================================
Results: 1 created, 2 skipped, 0 failed
==========================================
```

### 2️⃣ فحص القوالب عبر API

```bash
# عدد القيود المستحقة الآن
curl -s http://localhost:8001/api/recurring_templates/due_count

# النتيجة:
{"count": 0, "templates": []}
```

### 3️⃣ فحص آخر قيد تم إنشاؤه

```bash
# جلب آخر 10 قيود
curl -s http://localhost:8001/api/journal_entries?limit=10 | python3 -m json.tool
```

ابحث عن القيود التي `entry_type` = "دوري".

### 4️⃣ التحقق من Cron يعمل

```bash
# فحص سجل Cron (macOS)
tail -f /var/log/system.log | grep CRON

# فحص سجل Cron (Linux)
grep CRON /var/log/syslog
```

---

## 🔧 استكشاف الأخطاء

### مشكلة 1: Cron لا يعمل

**الأعراض:**
- لا يتم إنشاء قيود تلقائياً
- ملف `/tmp/recurring_journals.log` فارغ

**الحلول:**

**1. تحقق من تثبيت Cron**
```bash
crontab -l
```
يجب أن ترى السطر المضاف.

**2. تحقق من المسار**
```bash
cd /Users/salehalabbadi/yasargold/backend
pwd
```
تأكد أن المسار في crontab مطابق.

**3. تحقق من Python Environment**
```bash
cd /Users/salehalabbadi/yasargold/backend
source venv/bin/activate
python process_recurring_journals.py
```
يجب أن يعمل بدون أخطاء.

**4. تحقق من الصلاحيات**
```bash
chmod +x /Users/salehalabbadi/yasargold/backend/process_recurring_journals.py
```

**5. جرّب تشغيل يدوي لاختبار Cron**
```bash
# تعديل مؤقت لتشغيل كل دقيقة (للاختبار فقط!)
* * * * * cd /Users/salehalabbadi/yasargold/backend && source venv/bin/activate && python process_recurring_journals.py >> /tmp/recurring_journals.log 2>&1
```
انتظر دقيقة واحدة وافحص الملف:
```bash
cat /tmp/recurring_journals.log
```

**لا تنسى إعادته للجدول الطبيعي بعد الاختبار!**

---

### مشكلة 2: خطأ "ModuleNotFoundError"

**الأعراض:**
```
ModuleNotFoundError: No module named 'flask'
```

**السبب:** Cron لا يستخدم virtual environment

**الحل:** تأكد من سطر Cron يحتوي على:
```bash
source venv/bin/activate
```

أو استخدم المسار الكامل لـ Python:
```bash
0 1 * * * cd /Users/salehalabbadi/yasargold/backend && /Users/salehalabbadi/yasargold/backend/venv/bin/python process_recurring_journals.py >> /tmp/recurring_journals.log 2>&1
```

---

### مشكلة 3: قاعدة البيانات مقفلة

**الأعراض:**
```
sqlite3.OperationalError: database is locked
```

**السبب:** Backend server يعمل أثناء تشغيل Cron

**الحل:**
- SQLite لا يدعم كتابة متزامنة من عمليات متعددة
- **الخيار 1**: إيقاف Backend قبل تشغيل Cron (غير عملي)
- **الخيار 2**: استخدام PostgreSQL (مُوصى به للإنتاج)
- **الخيار 3**: تحديث توقيت Cron لوقت هادئ (مثلاً 3:00 ص)

---

### مشكلة 4: قيود تُنشأ مرتين

**الأعراض:**
- نفس القيد يُنشأ مرتين في نفس اليوم

**السبب:** تشغيل يدوي + تلقائي في نفس الوقت

**الحل:**
- النظام يتحقق من `next_run_date`
- إذا تم إنشاء قيد يدوياً، يُحدث `next_run_date` للموعد التالي
- لكن إن كان Cron يعمل في نفس اللحظة، قد يحدث تداخل

**الوقاية:**
```python
# النظام يستخدم معاملة قاعدة بيانات (transaction)
# لتجنب السباقات (race conditions)
db.session.commit()
```

---

## 🧪 اختبار النظام

### اختبار 1: تشغيل يدوي

```bash
cd /Users/salehalabbadi/yasargold/backend
source venv/bin/activate
python process_recurring_journals.py
```

**النتيجة المتوقعة:**
```
==========================================
Recurring Journals Processing Started
Time: 2025-11-08 15:30:45
==========================================
⏩ Skipped template 'راتب موظفي المحل' (next run: 2025-12-25)
⏩ Skipped template 'إيجار المحل' (next run: 2025-12-01)
⏩ Skipped template 'فواتير الخدمات' (next run: 2025-11-15)
⏩ Skipped template 'استهلاك الأصول' (next run: 2025-12-31)
==========================================
Results: 0 created, 4 skipped, 0 failed
==========================================
```

### اختبار 2: معالجة عبر API

```bash
curl -X POST http://localhost:8001/api/recurring_templates/process_all
```

**النتيجة:**
```json
{
  "message": "تمت معالجة القيود الدورية",
  "created": 0,
  "skipped": 4,
  "failed": 0
}
```

### اختبار 3: إنشاء قيد مباشر

```bash
curl -X POST http://localhost:8001/api/recurring_templates/1/create_entry
```

**النتيجة:**
```json
{
  "message": "تم إنشاء القيد بنجاح",
  "entry": {
    "id": 24,
    "entry_number": "JE-2025-00024",
    "date": "2025-11-08",
    "description": "رواتب الموظفين الشهرية (دوري - راتب موظفي المحل)"
  }
}
```

**تحقق من التحديث:**
```bash
curl -s http://localhost:8001/api/recurring_templates/1 | python3 -c "
import sys, json
data = json.load(sys.stdin)
print(f\"Total Created: {data['total_created']}\")
print(f\"Next Run: {data['next_run_date']}\")
print(f\"Last Created: {data['last_created_date']}\")
"
```

**النتيجة:**
```
Total Created: 2
Next Run: 2025-12-25T00:00:00
Last Created: 2025-11-08T00:00:00
```

---

## 📊 لوحة تحكم مقترحة (Dashboard)

### في الواجهة Flutter، يمكن إضافة:

**1. بطاقة إحصائيات:**
```
┌─────────────────────────────────┐
│  📊 إحصائيات القيود الدورية   │
├─────────────────────────────────┤
│  ✅ إجمالي القيود: 5            │
│  🔄 القوالب النشطة: 4           │
│  ⏰ المستحقة اليوم: 0           │
│  📅 القادمة هذا الشهر: 2        │
└─────────────────────────────────┘
```

**2. جدول الموعد التالي:**
```
┌────────────────────────────────────┐
│  📅 المواعيد القادمة              │
├────────────────────────────────────┤
│  15 نوفمبر  │ فواتير الخدمات      │
│  01 ديسمبر  │ إيجار المحل         │
│  25 ديسمبر  │ راتب الموظفين       │
│  31 ديسمبر  │ استهلاك الأصول      │
└────────────────────────────────────┘
```

**3. زر معالجة يدوية:**
```dart
ElevatedButton.icon(
  onPressed: () async {
    final result = await api.processAllDueTemplates();
    showDialog(
      context: context,
      builder: (context) => AlertDialog(
        title: Text('نتائج المعالجة'),
        content: Text('تم إنشاء ${result['created']} قيد'),
      ),
    );
  },
  icon: Icon(Icons.refresh),
  label: Text('معالجة القيود المستحقة'),
)
```

---

## ✅ قائمة التحقق النهائية

قبل الانتقال للإنتاج، تأكد من:

- [ ] Backend يعمل على `http://localhost:8001`
- [ ] جميع القوالب لها `is_active = true` و `auto_create = true`
- [ ] تاريخ `next_run_date` صحيح لكل قالب
- [ ] سطور القوالب متوازنة (مدين = دائن)
- [ ] Cron Job مثبت ويعمل
- [ ] ملف Log قابل للكتابة (`/tmp/recurring_journals.log`)
- [ ] Virtual Environment يعمل بشكل صحيح
- [ ] قاعدة البيانات غير مقفلة (أو استخدام PostgreSQL)
- [ ] الصلاحيات صحيحة للملفات والمجلدات
- [ ] تم اختبار النظام يدوياً بنجاح

---

## 📚 ملفات مرجعية

| الملف | الوصف |
|------|-------|
| `backend/process_recurring_journals.py` | سكريبت التشغيل التلقائي |
| `backend/recurring_journal_system.py` | المنطق الأساسي والنماذج |
| `backend/recurring_journal_routes.py` | API endpoints |
| `backend/setup_cron.sh` | مساعد إعداد Cron |
| `/tmp/recurring_journals.log` | سجل التشغيل |
| `RECURRING_JOURNAL_GUIDE.md` | دليل شامل للنظام |
| `RECURRING_UI_GUIDE_AR.md` | دليل الواجهة العربي |
| `FLUTTER_RECURRING_GUIDE.md` | دليل تطبيق Flutter |

---

## 🎯 الخلاصة

**النظام التلقائي يعمل كالتالي:**

1. **Cron** يشغل السكريبت يومياً الساعة 1:00 ص
2. **السكريبت** يفحص القوالب النشطة (`is_active=true`)
3. **للقوالب المستحقة** (`next_run_date <= today`)، ينشئ قيود جديدة
4. **يُحدث** `next_run_date` للموعد التالي تلقائياً
5. **يسجل** النتائج في `/tmp/recurring_journals.log`

**لا يحتاج تدخل بشري!** ✨

---

**للمساعدة أو الأسئلة:**
- راجع `RECURRING_JOURNAL_GUIDE.md`
- راجع سجل الأخطاء في `/tmp/recurring_journals.log`
- اختبر يدوياً باستخدام `python process_recurring_journals.py`

---

_تم بحمد الله_ 🚀
