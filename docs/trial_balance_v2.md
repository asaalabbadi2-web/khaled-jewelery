# ميزان المراجعة المحدث - Trial Balance V2

## 📋 نظرة عامة

تم تطوير وتحديث شاشة **ميزان المراجعة** بشكل كامل لتدعم النظام المزدوج (الذهب والنقد) بأفضل صورة ممكنة. الشاشة الجديدة توفر ميزات متقدمة مع واجهة احترافية وتصميم محسّن.

---

## ✨ الميزات الجديدة

### 1. **فلترة بالتاريخ**
- اختيار فترة زمنية محددة (من تاريخ / إلى تاريخ)
- عرض البيانات ضمن الفترة المحددة فقط
- إمكانية مسح الفلاتر بسهولة

### 2. **تفصيل العيارات**
- عرض تفصيلي لكل عيار (18K, 21K, 22K, 24K) بشكل منفصل
- أو عرض المجموع المحول إلى العيار الرئيسي (21K)
- التبديل بين العرضين بضغطة واحدة

### 3. **عرض الأرصدة**
- حساب الرصيد الصافي لكل حساب (المدين - الدائن)
- عرض الأرصدة بألوان متباينة (أخضر للموجب، أحمر للسالب)
- أرصدة منفصلة للذهب والنقد

### 4. **ملخص احترافي**
- بطاقات ملخص في أعلى الشاشة
- عرض إجماليات الذهب والنقد
- رموز توضيحية لسهولة الفهم
- تصميم متدرج الألوان مع ظلال

### 5. **تصميم محسّن**
- التزام بمعايير WCAG AA للتباين اللوني
- جداول قابلة للتمرير أفقياً وعمودياً
- رؤوس جداول ملونة ومميزة
- صف الإجماليات بخلفية زرقاء فاتحة

### 6. **معالجة الأخطاء**
- رسائل خطأ واضحة مع أيقونات
- زر إعادة المحاولة
- حالة تحميل مع مؤشر دائري

---

## 🔧 التغييرات التقنية

### Backend API (`/backend/routes.py`)

#### Endpoint: `/api/trial_balance`
**Query Parameters:**
- `start_date` (optional): تاريخ البداية بصيغة YYYY-MM-DD
- `end_date` (optional): تاريخ النهاية بصيغة YYYY-MM-DD
- `karat_detail` (optional): `true` لعرض تفاصيل العيارات، `false` للمجموع المحول

**Response (Normal Mode):**
```json
{
  "trial_balance": [
    {
      "account_id": 1,
      "account_number": "1010",
      "account_name": "الصندوق",
      "gold_debit": 150.500,
      "gold_credit": 100.250,
      "gold_balance": 50.250,
      "cash_debit": 50000.00,
      "cash_credit": 30000.00,
      "cash_balance": 20000.00
    }
  ],
  "totals": {
    "gold_debit": 150.500,
    "gold_credit": 100.250,
    "gold_balance": 50.250,
    "cash_debit": 50000.00,
    "cash_credit": 30000.00,
    "cash_balance": 20000.00
  },
  "filters": {
    "start_date": "2024-01-01",
    "end_date": "2024-12-31",
    "karat_detail": false
  },
  "count": 15
}
```

**Response (Karat Detail Mode):**
```json
{
  "trial_balance": [
    {
      "account_id": 1,
      "account_number": "1010",
      "account_name": "الصندوق",
      "debit_18k": 10.500,
      "credit_18k": 5.250,
      "balance_18k": 5.250,
      "debit_21k": 100.000,
      "credit_21k": 80.000,
      "balance_21k": 20.000,
      "debit_22k": 20.000,
      "credit_22k": 10.000,
      "balance_22k": 10.000,
      "debit_24k": 15.000,
      "credit_24k": 5.000,
      "balance_24k": 10.000,
      "cash_debit": 50000.00,
      "cash_credit": 30000.00,
      "cash_balance": 20000.00
    }
  ],
  "totals": {
    "debit_18k": 10.500,
    "credit_18k": 5.250,
    "balance_18k": 5.250,
    "debit_21k": 100.000,
    "credit_21k": 80.000,
    "balance_21k": 20.000,
    "debit_22k": 20.000,
    "credit_22k": 10.000,
    "balance_22k": 10.000,
    "debit_24k": 15.000,
    "credit_24k": 5.000,
    "balance_24k": 10.000,
    "cash_debit": 50000.00,
    "cash_credit": 30000.00,
    "cash_balance": 20000.00
  },
  "filters": {
    "start_date": null,
    "end_date": null,
    "karat_detail": true
  },
  "count": 15
}
```

### Frontend API Service (`/frontend/lib/api_service.dart`)

```dart
Future<Map<String, dynamic>> getTrialBalance({
  String? startDate,      // تاريخ البداية (اختياري)
  String? endDate,        // تاريخ النهاية (اختياري)
  bool karatDetail = false, // إظهار تفاصيل العيارات
}) async
```

**مثال الاستخدام:**
```dart
// عرض عادي (مجموع محول)
final data = await ApiService().getTrialBalance();

// عرض مع فلترة بالتاريخ
final filteredData = await ApiService().getTrialBalance(
  startDate: '2024-01-01',
  endDate: '2024-12-31',
);

// عرض تفاصيل العيارات
final detailedData = await ApiService().getTrialBalance(
  karatDetail: true,
);

// كل الخيارات معاً
final fullData = await ApiService().getTrialBalance(
  startDate: '2024-01-01',
  endDate: '2024-12-31',
  karatDetail: true,
);
```

---

## 🎨 مكونات الواجهة

### 1. شريط الفلاتر (`_buildFilterSummary`)
- يظهر فقط عند وجود فلاتر نشطة
- خلفية زرقاء داكنة (Colors.blue.shade700)
- شرائح (Chips) لكل فلتر مع إمكانية الإزالة الفردية
- زر "مسح الكل" لإزالة جميع الفلاتر

### 2. بطاقات الملخص (`_buildSummaryCards`)

#### العرض العادي (`_buildNormalSummary`)
- بطاقة الذهب: عيار 21 محول
- بطاقة النقد: بالعملة المحلية
- كل بطاقة تعرض: المدين، الدائن، الرصيد

#### عرض تفاصيل العيارات (`_buildKaratDetailSummary`)
- رقائق (Chips) لكل عيار مع الرصيد
- بطاقة النقد منفصلة
- ألوان ذهبية (amber) للتمييز

### 3. الجداول

#### الجدول العادي (`_buildNormalTable`)
**الأعمدة:**
1. رقم الحساب
2. اسم الحساب
3. مدين ذهب (21K)
4. دائن ذهب (21K)
5. **رصيد ذهب** ← جديد
6. مدين نقد
7. دائن نقد
8. **رصيد نقد** ← جديد

#### جدول تفاصيل العيارات (`_buildKaratDetailTable`)
**الأعمدة:** (17 عمود)
1. رقم الحساب
2. اسم الحساب
3-5. 18K (مدين، دائن، رصيد)
6-8. 21K (مدين، دائن، رصيد)
9-11. 22K (مدين، دائن، رصيد)
12-14. 24K (مدين، دائن، رصيد)
15-17. النقد (مدين، دائن، رصيد)

### 4. حوار الفلترة (`_showFilterDialog`)
**الأقسام:**

#### الفترة الزمنية
- منتقي تاريخ البداية مع زر المسح
- منتقي تاريخ النهاية مع زر المسح
- تقويم عربي

#### خيارات العرض
- مفتاح تبديل (Switch) لتفاصيل العيارات
- نص توضيحي

**الأزرار:**
- إلغاء (رمادي)
- تطبيق (أزرق مع أيقونة ✓)

---

## 🎯 قواعد التصميم المطبقة

### 1. **التباين اللوني (WCAG AA)**
- رؤوس الجداول: رمادي فاتح (grey.shade200) + أزرق داكن (#1976D2)
- شريط الفلاتر: أزرق داكن (blue.shade700) + نص أبيض
- الأرصدة الموجبة: أخضر داكن (green.shade700)
- الأرصدة السالبة: أحمر داكن (red.shade700)

### 2. **التسلسل البصري**
```
AppBar (أزرق داكن)
  ↓
شريط الفلاتر (أزرق داكن) [اختياري]
  ↓
بطاقات الملخص (تدرجات + حدود)
  ↓
الجدول (رؤوس رمادية + محتوى أبيض)
  ↓
صف الإجماليات (خلفية زرقاء فاتحة)
```

### 3. **الرموز التوضيحية**
- `Icons.filter_alt` → الفلاتر
- `Icons.refresh` → تحديث
- `Icons.savings` → الذهب
- `Icons.account_balance_wallet` → النقد
- `Icons.calendar_today` → التاريخ
- `Icons.details` → التفاصيل

---

## 🔄 سير العمل (Workflow)

### 1. التحميل الأولي
```
initState()
  ↓
_loadTrialBalance()
  ↓
ApiService.getTrialBalance()
  ↓
setState() → عرض البيانات
```

### 2. تطبيق الفلاتر
```
المستخدم يفتح حوار الفلترة
  ↓
يختار التواريخ / يُفعّل تفاصيل العيارات
  ↓
يضغط "تطبيق"
  ↓
setState() مع القيم الجديدة
  ↓
_loadTrialBalance() بالمعاملات
  ↓
عرض البيانات المفلترة
```

### 3. إزالة فلتر واحد
```
المستخدم يضغط X على chip
  ↓
setState() → إزالة القيمة
  ↓
_loadTrialBalance() بدون هذا الفلتر
```

### 4. مسح كل الفلاتر
```
المستخدم يضغط "مسح الكل"
  ↓
_clearFilters()
  ↓
setState() → null لكل الفلاتر
  ↓
_loadTrialBalance() بدون معاملات
```

---

## 📊 أمثلة الاستخدام

### مثال 1: ميزان مراجعة لشهر معين
```dart
// في الكود
final januaryData = await ApiService().getTrialBalance(
  startDate: '2024-01-01',
  endDate: '2024-01-31',
);

// من الواجهة
1. افتح ميزان المراجعة
2. اضغط أيقونة الفلتر
3. اختر "من تاريخ": 1 يناير 2024
4. اختر "إلى تاريخ": 31 يناير 2024
5. اضغط "تطبيق"
```

### مثال 2: مراجعة تفاصيل عيار معين
```dart
// في الكود
final karatDetails = await ApiService().getTrialBalance(
  karatDetail: true,
);

// من الواجهة
1. افتح ميزان المراجعة
2. اضغط أيقونة الفلتر
3. فعّل "إظهار تفاصيل العيارات"
4. اضغط "تطبيق"
5. ستظهر 4 أعمدة لكل عيار (18K, 21K, 22K, 24K)
```

### مثال 3: تحليل ربع سنوي بتفصيل العيارات
```dart
final q1Analysis = await ApiService().getTrialBalance(
  startDate: '2024-01-01',
  endDate: '2024-03-31',
  karatDetail: true,
);
```

---

## 🐛 معالجة الحالات الخاصة

### 1. لا توجد بيانات
- رسالة: "لا توجد بيانات"
- لون رمادي وسط الشاشة

### 2. خطأ في التحميل
```dart
Widget _buildErrorWidget() {
  return Center(
    child: Column(
      children: [
        Icon(Icons.error_outline, size: 64, color: red.shade300),
        Text('خطأ في تحميل البيانات: ...'),
        ElevatedButton('إعادة المحاولة'),
      ],
    ),
  );
}
```

### 3. حالة التحميل
```dart
_isLoading
  ? Center(child: CircularProgressIndicator())
  : _buildTrialBalanceTable()
```

### 4. فلاتر فارغة
- إخفاء شريط الفلاتر
- عرض كل البيانات

---

## 🎭 إدارة الحالة (State Management)

### المتغيرات الرئيسية
```dart
DateTime? _startDate;         // تاريخ البداية
DateTime? _endDate;           // تاريخ النهاية
bool _showKaratDetail;        // إظهار تفاصيل العيارات
List<Map<String, dynamic>> _entries; // قائمة الحسابات
Map<String, dynamic> _totals; // الإجماليات
bool _isLoading;              // حالة التحميل
String? _errorMessage;        // رسالة الخطأ
```

### نمط StatefulBuilder للحوارات
```dart
showDialog(
  builder: (context) => StatefulBuilder(
    builder: (context, setDialogState) => AlertDialog(
      // استخدام setDialogState للتحديث داخل الحوار
      // استخدام setState للتحديث في الشاشة الأم
    ),
  ),
);
```

---

## 🚀 التحسينات المستقبلية المقترحة

### 1. التصدير
- [ ] تصدير PDF بتنسيق احترافي
- [ ] تصدير Excel مع صيغ الحسابات
- [ ] تصدير CSV للتحليل الخارجي

### 2. الطباعة
- [ ] معاينة قبل الطباعة
- [ ] تنسيق مناسب للورق A4
- [ ] اختيار ما يُطبع (مع/بدون تفاصيل)

### 3. الرسوم البيانية
- [ ] رسم بياني دائري للحسابات
- [ ] رسم بياني خطي للأرصدة عبر الزمن
- [ ] مقارنة الفترات

### 4. البحث والفرز
- [ ] بحث عن حساب محدد
- [ ] فرز حسب الاسم / الرقم / الرصيد
- [ ] فلترة حسب نوع الحساب

### 5. الحفظ والمشاركة
- [ ] حفظ إعدادات الفلترة المفضلة
- [ ] مشاركة التقرير عبر البريد
- [ ] جدولة تقارير دورية

---

## 📁 الملفات المعدّلة

### Backend
- ✅ `/backend/routes.py` - تحديث `/api/trial_balance`

### Frontend
- ✅ `/frontend/lib/api_service.dart` - إضافة معاملات للـ `getTrialBalance()`
- ✅ `/frontend/lib/screens/trial_balance_screen_v2.dart` - الشاشة الجديدة (700+ سطر)
- ✅ `/frontend/lib/screens/home_screen.dart` - تحديث الإشارة للشاشة الجديدة

### Documentation
- ✅ `/docs/trial_balance_v2.md` - هذا الملف

---

## ✅ قائمة التحقق (Checklist)

- [x] تحديث Backend API بمعاملات الفلترة
- [x] إضافة دعم تفاصيل العيارات في API
- [x] تحديث ApiService في Flutter
- [x] إنشاء TrialBalanceScreenV2
- [x] إضافة فلاتر التاريخ
- [x] إضافة تبديل تفاصيل العيارات
- [x] حساب وعرض الأرصدة
- [x] تصميم بطاقات الملخص
- [x] تطبيق معايير WCAG AA
- [x] معالجة الأخطاء والحالات الخاصة
- [x] تحديث HomeScreen للإشارة للشاشة الجديدة
- [x] إنشاء وثائق شاملة
- [ ] اختبار المستخدم النهائي
- [ ] إضافة ميزات التصدير (مستقبلي)

---

## 📞 الدعم والمساعدة

للأسئلة أو المساعدة:
1. راجع هذا الملف أولاً
2. تحقق من ملفات التوثيق الأخرى في `/docs/`
3. افحص الكود المصدري مع التعليقات التوضيحية

**آخر تحديث:** 16 أكتوبر 2025
**الإصدار:** 2.0.0
**المطور:** Yasar Gold & Jewelry POS Team
