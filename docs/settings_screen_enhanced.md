# شاشة الإعدادات المحسّنة 🎨

## نظرة عامة
تم تطوير نسخة محسّنة من شاشة الإعدادات العامة بتصميم احترافي حديث يركز على:
- تحسين الألوان والتدرجات اللونية
- تنظيم المعلومات في بطاقات واضحة
- إضافة أيقونات تعبيرية
- تجربة مستخدم أفضل

## الملف الجديد
```
frontend/lib/screens/settings_screen_enhanced.dart
```

## المميزات الجديدة

### 1. تصميم عصري احترافي
- **تدرجات لونية**: استخدام Gradient في AppBar والبطاقات
- **نظام ألوان متسق**:
  - 🟡 ذهبي (#FFD700): العيار والذهب
  - 🔵 أزرق (#1976D2): إعدادات عامة
  - 🟢 أخضر (#4CAF50): نجاح وتأكيد
  - 🟠 برتقالي (#FF9800): تحذيرات وضريبة
  - 🔴 أحمر (#F44336): أخطاء ورقم ضريبي

### 2. تحسينات بصرية للتبويبات

#### تبويب "عام" (General Tab)
**التحسينات:**
- ✅ بطاقات منفصلة لكل قسم (ذهب، عملة، دقة)
- ✅ أيقونات ملونة مميزة لكل قسم
- ✅ معلومات تفصيلية عن العيارات مع نسبة النقاء
- ✅ اختيار المنازل العشرية بواجهة تفاعلية
- ✅ نصائح سريعة في نهاية الشاشة

**مثال كود بطاقة القسم:**
```dart
_buildSectionCard(
  title: 'إعدادات الذهب',
  icon: Icons.diamond,
  iconColor: _primaryColor,
  children: [
    // المحتوى
  ],
)
```

#### تبويب "الضريبة" (Tax Tab)
**التحسينات:**
- ✅ مفتاح تشغيل/إيقاف بتصميم بصري واضح
- ✅ شريط تمرير (Slider) لتحديد نسبة الضريبة
- ✅ عرض رقم كبير للنسبة المختارة
- ✅ أمثلة حسابية تفاعلية للضريبة على مبالغ مختلفة
- ✅ تأثيرات بصرية عند التفعيل/التعطيل

**العناصر التفاعلية:**
- مفتاح Switch ملون
- Slider بتدرج من 0% إلى 30%
- أمثلة حسابية ديناميكية تتحدث مع تغيير النسبة

#### تبويب "الشركة" (Company Tab)
**التحسينات:**
- ✅ حقول إدخال ملونة حسب نوع البيانات
- ✅ أيقونات توضيحية لكل حقل
- ✅ معاينة مباشرة لرأس الفاتورة
- ✅ تحديث فوري للمعاينة عند الكتابة

**حقول الإدخال:**
- 🔵 اسم الشركة (business_center)
- 🟢 رقم الهاتف (phone)
- 🟠 العنوان (location_on)
- 🔴 الرقم الضريبي (badge)

#### تبويب "طرق الدفع" (Payment Methods)
**التحسينات:**
- ✅ عرض قائمة بطرق الدفع المسجلة
- ✅ بطاقات ملونة لكل طريقة دفع
- ✅ شارات حالة (نشط/غير نشط)
- ✅ رسالة واضحة عند عدم وجود طرق دفع

## نظام الألوان المستخدم

### ألوان أساسية:
```dart
final Color _primaryColor = const Color(0xFFFFD700);  // ذهبي
final Color _accentColor = const Color(0xFF1976D2);   // أزرق
final Color _successColor = const Color(0xFF4CAF50);  // أخضر
final Color _warningColor = const Color(0xFFFF9800);  // برتقالي
final Color _errorColor = const Color(0xFFF44336);    // أحمر
```

### استخدام الألوان:
- **ذهبي**: العيار، أيقونات الذهب، AppBar gradient
- **أزرق**: إعدادات عامة، النصائح، معلومات الشركة
- **أخضر**: زر الحفظ، نجاح العمليات، طرق الدفع النشطة
- **برتقالي**: الضريبة، التحذيرات، العنوان
- **أحمر**: الأخطاء، الرقم الضريبي

## عناصر التصميم الرئيسية

### 1. بطاقة القسم (Section Card)
```dart
Widget _buildSectionCard({
  required String title,
  required IconData icon,
  required Color iconColor,
  required List<Widget> children,
})
```
- تدرج لوني في الخلفية
- أيقونة ملونة في دائرة
- عنوان بخط عريض
- محتوى مخصص

### 2. نص معلوماتي (Info Text)
```dart
Widget _buildInfoText(String text)
```
- خلفية رمادية فاتحة
- أيقونة معلومات
- نص توضيحي

### 3. معلومات العيارات (Karat Info)
- قائمة بجميع العيارات (18، 21، 22، 24)
- نقاط ملونة لكل عيار
- نسبة النقاء بجانب كل عيار
- شارة "محدد" للعيار النشط

### 4. اختيار المنازل العشرية
- ثلاثة أزرار كبيرة (2، 3، 4 منازل)
- تأثيرات بصرية عند الاختيار
- مثال توضيحي ديناميكي

### 5. نصائح سريعة (Quick Info)
- بطاقة بتدرج أزرق
- رموز تعبيرية (emoji)
- نصائح مختصرة ومفيدة

## التعديلات في AppBar

### التصميم الجديد:
```dart
AppBar(
  flexibleSpace: Container(
    decoration: BoxDecoration(
      gradient: LinearGradient(
        colors: [_primaryColor, Color.lerp(_primaryColor, _accentColor, 0.3)!],
        begin: Alignment.topLeft,
        end: Alignment.bottomRight,
      ),
    ),
  ),
  title: Row(
    children: [
      Icon(Icons.settings, color: Colors.white, size: 28),
      SizedBox(width: 12),
      Text('الإعدادات العامة', ...),
    ],
  ),
  actions: [
    ElevatedButton.icon(
      onPressed: _saveSettings,
      icon: Icon(Icons.save),
      label: Text('حفظ'),
      style: ElevatedButton.styleFrom(
        backgroundColor: _successColor,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(25),
        ),
      ),
    ),
  ],
)
```

## TabBar المحسّن

### التصميم:
- خلفية بيضاء مع ظل خفيف
- أيقونات أكبر (22px)
- خط مؤشر ذهبي (indicatorColor)
- ألوان نشطة/غير نشطة واضحة

## الرسائل التوضيحية (SnackBars)

### التحسينات:
```dart
SnackBar(
  content: Row(
    children: [
      Icon(isError ? Icons.error_outline : Icons.check_circle_outline),
      SizedBox(width: 12),
      Expanded(child: Text(message)),
    ],
  ),
  backgroundColor: isError ? _errorColor : _successColor,
  behavior: SnackBarBehavior.floating,
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
)
```

## حالة التحميل (Loading State)

### واجهة الانتظار:
- دائرة تحميل ملونة بالذهبي
- رسالة "جاري تحميل الإعدادات..."
- تصميم مركزي أنيق

## كيفية الاستخدام

### 1. الانتقال للشاشة المحسّنة:
```dart
// في home_screen.dart
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (_) => SettingsScreenEnhanced(),
  ),
);
```

### 2. استبدال الشاشة القديمة:
يمكن استبدال `SettingsScreen` بـ `SettingsScreenEnhanced` في جميع أماكن الاستخدام.

### 3. التخصيص:
يمكن تعديل الألوان الأساسية من خلال تغيير القيم في `initState`:
```dart
final Color _primaryColor = const Color(0xFFFFD700);  // غيّر هنا
```

## الفروقات عن الشاشة القديمة

| الميزة | القديمة | المحسّنة |
|--------|---------|----------|
| تصميم البطاقات | بسيط أبيض | تدرجات وألوان |
| الأيقونات | محدودة | شاملة وملونة |
| المعلومات التوضيحية | غير موجودة | موجودة في كل قسم |
| التفاعل البصري | محدود | واسع (Slider, Switch, Preview) |
| نظام الألوان | موحد | متنوع ومنظم |
| المعاينة المباشرة | غير موجودة | معاينة رأس الفاتورة |
| الأمثلة الحسابية | غير موجودة | أمثلة ديناميكية للضريبة |

## الاختبارات المطلوبة

### قائمة فحص:
- [ ] اختبار حفظ الإعدادات
- [ ] التأكد من تحميل البيانات الصحيحة
- [ ] اختبار التنقل بين التبويبات
- [ ] فحص الألوان على أجهزة مختلفة
- [ ] التأكد من عمل Slider الضريبة
- [ ] فحص معاينة رأس الفاتورة
- [ ] اختبار حالة التحميل
- [ ] التأكد من ظهور SnackBars بشكل صحيح

## الأخطاء المعروفة
لا توجد أخطاء معروفة حالياً.

## الملاحظات الفنية

### الأداء:
- استخدام `AnimatedOpacity` للتأثيرات الانتقالية
- تحميل البيانات بشكل غير متزامن
- إدارة الحالة بكفاءة عبر `setState`

### إمكانية الوصول:
- ألوان بتباين واضح
- أيقونات توضيحية
- نصوص قابلة للقراءة

### التوافق:
- يعمل على جميع منصات Flutter (iOS, Android, Web, Desktop)
- يدعم RTL بشكل كامل
- يتكيف مع أحجام شاشات مختلفة

## التطويرات المستقبلية المقترحة
- [ ] إضافة وضع داكن (Dark Mode)
- [ ] دعم لغات إضافية
- [ ] معاينة كاملة للفاتورة (وليس فقط الرأس)
- [ ] إعدادات متقدمة للطباعة
- [ ] نسخ احتياطي/استرجاع الإعدادات

---

**آخر تحديث:** 2024
**الإصدار:** 1.0
**الحالة:** ✅ جاهز للاستخدام
