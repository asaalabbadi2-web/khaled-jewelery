# 📋 نظام السندات - دليل الاستخدام

## 📖 المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [الميزات الرئيسية](#الميزات-الرئيسية)
3. [دليل الاستخدام](#دليل-الاستخدام)
4. [API Documentation](#api-documentation)
5. [التقنيات المستخدمة](#التقنيات-المستخدمة)
6. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## 🎯 نظرة عامة

**نظام السندات** هو جزء من نظام **Yasar Gold & Jewelry POS** لإدارة:
- 📥 **سندات القبض** (Receipt Vouchers) - توثيق استلام الأموال من العملاء
- 📤 **سندات الصرف** (Payment Vouchers) - توثيق دفع الأموال للموردين
- ⚖️ **سندات التسوية** (Adjustment Vouchers) - تسويات محاسبية

### لماذا نحتاج السندات؟
- ✅ توثيق كل حركة مالية
- ✅ تنظيم التعاملات مع العملاء والموردين
- ✅ الربط مع النظام المحاسبي
- ✅ التدقيق والمراجعة المالية

---

## ⚡ الميزات الرئيسية

### 📱 **واجهة المستخدم**
- ✅ تصميم عربي احترافي (RTL)
- ✅ واجهة سهلة وبسيطة
- ✅ ألوان مميزة لكل نوع سند
- ✅ بحث وفلترة متقدمة

### 🔧 **الوظائف**
- ✅ إنشاء سند قبض/صرف
- ✅ عرض قائمة السندات مع فلاتر
- ✅ البحث برقم السند أو البيان
- ✅ عرض تفاصيل السند الكاملة
- ✅ إلغاء السند (مع سبب الإلغاء)
- ✅ حذف السند
- ✅ إحصائيات السندات

### 💰 **دعم المبالغ**
- ✅ مبالغ نقدية (ر.س)
- ✅ مبالغ ذهبية (غرام + عيار)
- ✅ إمكانية إدخال كلا النوعين معاً

### 🔗 **الربط والتكامل**
- ✅ ربط السند بفاتورة
- ✅ ربط السند بقيد محاسبي
- ✅ ربط السند بعميل/مورد

---

## 📚 دليل الاستخدام

### 1️⃣ **الوصول إلى السندات**

#### من الشاشة الرئيسية:
1. افتح **القائمة الجانبية** (☰)
2. انتقل إلى قسم **"💰 المحاسبة"**
3. اختر:
   - **"📋 السندات"** - لعرض القائمة
   - **"📥 سند قبض"** - لإنشاء قبض
   - **"📤 سند صرف"** - لإنشاء صرف

#### أو استخدم الأزرار السريعة:
- زر **"سند قبض"** 🟢 (أخضر)
- زر **"سند صرف"** 🔴 (أحمر)

---

### 2️⃣ **إنشاء سند قبض** 📥

#### الخطوات:
1. **اضغط على** "سند قبض"
2. **اختر التاريخ** (افتراضياً: اليوم)
3. **حدد الطرف:**
   - عميل (اختر من القائمة)
   - مورد (اختر من القائمة)
   - آخر (أدخل الاسم يدوياً)
4. **أدخل المبلغ:**
   - المبلغ النقدي (ر.س) - اختياري
   - المبلغ الذهبي (غرام + عيار) - اختياري
   - **ملاحظة:** يجب إدخال مبلغ واحد على الأقل
5. **حدد المرجع** (اختياري):
   - يدوي (بدون مرجع)
   - فاتورة (اختر من القائمة)
   - قيد محاسبي
6. **اكتب البيان** (مطلوب)
   - مثال: "سداد فاتورة رقم 123"
7. **أضف ملاحظات** (اختياري)
8. **اضغط "حفظ السند"** ✅

#### نتيجة:
- ✅ السند يُنشأ برقم تلقائي: `RV-2025-00001`
- ✅ يظهر في قائمة السندات
- ✅ يمكن طباعته وإرساله للعميل

---

### 3️⃣ **إنشاء سند صرف** 📤

نفس خطوات سند القبض، لكن:
- 🔴 لون أحمر (للدفع)
- الطرف غالباً **مورد**
- رقم تلقائي: `PV-2025-00001`

---

### 4️⃣ **عرض قائمة السندات** 📋

#### الشاشة تحتوي على:
1. **Tabs (تبويبات):**
   - **الكل** - جميع السندات
   - **📥 قبض** - سندات القبض فقط
   - **📤 صرف** - سندات الصرف فقط

2. **بطاقة الإحصائيات:**
   - الإجمالي (عدد السندات)
   - عدد سندات القبض
   - عدد سندات الصرف
   - عدد السندات النشطة

3. **شريط البحث:**
   - ابحث برقم السند: `RV-2025-00001`
   - ابحث بالبيان: `سداد دين`

4. **زر الفلاتر:**
   - فلترة حسب الحالة (نشط/ملغى)
   - فلترة حسب الفترة الزمنية (من تاريخ → إلى تاريخ)

5. **قائمة السندات:**
   - بطاقة لكل سند تعرض:
     - رقم السند ونوعه
     - التاريخ
     - اسم الطرف (عميل/مورد)
     - المبلغ (نقدي/ذهبي)
     - البيان
     - حالة الإلغاء (إن وُجد)
   - أزرار الإجراءات:
     - **إلغاء** - إلغاء السند مع سبب
     - **حذف** - حذف السند نهائياً

#### الإجراءات:
- **اضغط على سند** → عرض التفاصيل
- **اسحب للأسفل** → تحديث القائمة (Pull to Refresh)

---

### 5️⃣ **عرض تفاصيل السند** 📄

#### الشاشة تعرض:
1. **بطاقة معلومات السند:**
   - رقم السند
   - نوع السند (قبض/صرف)
   - التاريخ
   - حالة الإلغاء (إن وُجد)

2. **بطاقة المبلغ:**
   - المبلغ النقدي (إن وُجد)
   - المبلغ الذهبي + العيار (إن وُجد)

3. **بطاقة الطرف:**
   - نوع الطرف (عميل/مورد/آخر)
   - اسم الطرف

4. **بطاقة البيان:**
   - البيان الكامل

5. **بطاقة المرجع** (إن وُجد):
   - نوع المرجع (فاتورة/قيد)
   - رقم المرجع

6. **بطاقة الملاحظات** (إن وُجدت)

7. **بطاقة البيانات الوصفية:**
   - تاريخ الإنشاء
   - آخر تحديث
   - اسم المستخدم

#### الإجراءات:
- **زر إلغاء** (في AppBar) - إلغاء السند
- **زر حذف** (في AppBar) - حذف السند
- **زر طباعة** (قريباً)

---

### 6️⃣ **إلغاء سند** ❌

#### الخطوات:
1. افتح تفاصيل السند أو من قائمة السندات
2. اضغط **"إلغاء"**
3. **أدخل سبب الإلغاء** (مطلوب)
   - مثال: "خطأ في المبلغ"
   - مثال: "إلغاء بناءً على طلب العميل"
4. اضغط **"إلغاء السند"**

#### نتيجة:
- ✅ حالة السند تتغير إلى **"ملغى"**
- ✅ السبب يُحفظ ويظهر في التفاصيل
- ✅ لا يمكن تعديل أو حذف السند الملغى
- ⚠️ السند الملغى يبقى في السجلات للمراجعة

---

### 7️⃣ **حذف سند** 🗑️

#### الخطوات:
1. افتح تفاصيل السند أو من قائمة السندات
2. اضغط **"حذف"**
3. تأكيد الحذف

#### شروط الحذف:
- ✅ السند **غير ملغى**
- ✅ السند **غير مرتبط** بقيد محاسبي
- ⚠️ إذا كان السند مرتبط بقيد، يجب حذف القيد أولاً

#### نتيجة:
- السند يُحذف **نهائياً** من القاعدة

---

## 🔌 API Documentation

### Base URL
```
http://127.0.0.1:8001/api
```

### Endpoints

#### 1. **Get Vouchers**
```http
GET /vouchers
```

**Query Parameters:**
- `type` - نوع السند (receipt, payment, adjustment)
- `party_type` - نوع الطرف (customer, supplier, other)
- `status` - الحالة (active, cancelled)
- `date_from` - من تاريخ (YYYY-MM-DD)
- `date_to` - إلى تاريخ (YYYY-MM-DD)
- `customer_id` - معرف العميل
- `supplier_id` - معرف المورد

**Response:**
```json
[
  {
    "id": 1,
    "voucher_number": "RV-2025-00001",
    "voucher_type": "receipt",
    "date": "2025-10-10",
    "party_type": "customer",
    "customer_id": 1,
    "customer": {
      "id": 1,
      "name": "أحمد محمد"
    },
    "amount_cash": 1000.00,
    "amount_gold": 0.0,
    "gold_karat": null,
    "description": "سداد فاتورة رقم 123",
    "status": "active",
    "created_at": "2025-10-10T10:30:00"
  }
]
```

---

#### 2. **Get Voucher by ID**
```http
GET /vouchers/:id
```

**Response:**
```json
{
  "id": 1,
  "voucher_number": "RV-2025-00001",
  "voucher_type": "receipt",
  ...
}
```

---

#### 3. **Create Voucher**
```http
POST /vouchers
Content-Type: application/json
```

**Request Body:**
```json
{
  "voucher_type": "receipt",
  "date": "2025-10-10",
  "party_type": "customer",
  "customer_id": 1,
  "amount_cash": 1000.00,
  "amount_gold": 0.0,
  "description": "سداد دين",
  "reference_type": "manual",
  "notes": "ملاحظات إضافية"
}
```

**Response:**
```json
{
  "id": 1,
  "voucher_number": "RV-2025-00001",
  "message": "Voucher created successfully"
}
```

---

#### 4. **Update Voucher**
```http
PUT /vouchers/:id
Content-Type: application/json
```

**Request Body:**
```json
{
  "amount_cash": 1500.00,
  "description": "سداد دين محدّث"
}
```

---

#### 5. **Delete Voucher**
```http
DELETE /vouchers/:id
```

**Response:**
```json
{
  "message": "Voucher deleted successfully"
}
```

---

#### 6. **Cancel Voucher**
```http
POST /vouchers/:id/cancel
Content-Type: application/json
```

**Request Body:**
```json
{
  "reason": "خطأ في المبلغ"
}
```

**Response:**
```json
{
  "message": "Voucher cancelled successfully",
  "voucher": { ... }
}
```

---

#### 7. **Get Vouchers Stats**
```http
GET /vouchers/stats
```

**Response:**
```json
{
  "total_count": 50,
  "receipt_count": 30,
  "payment_count": 18,
  "adjustment_count": 2,
  "active_count": 45,
  "cancelled_count": 5,
  "total_cash_in": 50000.00,
  "total_cash_out": 30000.00,
  "total_gold_in": 100.5,
  "total_gold_out": 50.25
}
```

---

## 🛠️ التقنيات المستخدمة

### Backend
- **Framework:** Flask 3.0.3
- **Database:** SQLAlchemy ORM + SQLite/PostgreSQL
- **Migrations:** Alembic
- **Python:** 3.9+

### Frontend
- **Framework:** Flutter 3.x
- **Language:** Dart
- **HTTP Client:** http package
- **State Management:** StatefulWidget
- **UI:** Material Design 3

### Features
- ✅ RESTful API
- ✅ JSON serialization
- ✅ UTF-8 encoding (Arabic support)
- ✅ Error handling
- ✅ Input validation
- ✅ Auto-numbering system
- ✅ Soft delete (cancellation)

---

## ❓ الأسئلة الشائعة

### 1. ما الفرق بين سند القبض وسند الصرف؟
- **سند القبض** 📥: يُستخدم عند **استلام** أموال من عميل
- **سند الصرف** 📤: يُستخدم عند **دفع** أموال لمورد

### 2. هل يمكن تعديل السند بعد الحفظ؟
- ✅ **نعم** - إذا كان السند **نشط** (غير ملغى)
- ❌ **لا** - إذا كان السند **ملغى**

### 3. ماذا يحدث عند إلغاء السند؟
- السند يتحول لحالة "ملغى"
- لا يمكن تعديله أو حذفه
- يبقى في السجلات للمراجعة
- سبب الإلغاء يُحفظ

### 4. هل يمكن إدخال مبلغ نقدي وذهبي معاً؟
- ✅ **نعم** - يمكن إدخال كليهما في نفس السند
- ⚠️ يجب إدخال مبلغ واحد على الأقل

### 5. ما هو رقم السند التلقائي؟
- رقم يُولّد تلقائياً بصيغة: `RV-2025-00001`
  - `RV` = Receipt Voucher (سند قبض)
  - `PV` = Payment Voucher (سند صرف)
  - `AV` = Adjustment Voucher (سند تسوية)
  - `2025` = السنة
  - `00001` = رقم تسلسلي

### 6. هل يمكن ربط السند بفاتورة؟
- ✅ **نعم** - اختر "فاتورة" في حقل المرجع
- يساعد في تتبع المدفوعات

### 7. ما هي استخدامات المرجع "قيد محاسبي"؟
- ربط السند بقيد محاسبي مباشرة
- مفيد للتسويات المحاسبية
- (قريباً: إنشاء قيد تلقائي عند إنشاء السند)

### 8. كيف أبحث عن سند معين؟
- ابحث برقم السند: `RV-2025-00001`
- ابحث بالبيان: `سداد دين`
- استخدم الفلاتر: تاريخ، حالة، نوع

### 9. ماذا يحدث عند حذف السند؟
- السند يُحذف **نهائياً**
- ⚠️ لا يمكن التراجع
- ✅ فقط إذا لم يكن مرتبطاً بقيد

### 10. هل هناك ميزات قادمة؟
قريباً:
- 🖨️ طباعة السندات (PDF)
- 📎 رفع مرفقات (صور/ملفات)
- 📊 تقارير متقدمة
- 🔔 إشعارات
- 📝 إنشاء قيود تلقائياً
- ✅ موافقات متعددة المستويات

---

## 📞 الدعم والمساعدة

إذا واجهتك أي مشكلة:
1. تحقق من **الشروط** (مثلاً: مبلغ > 0)
2. تأكد من **الإنترنت** (للاتصال بالـ Backend)
3. راجع **رسالة الخطأ** (تظهر في SnackBar)
4. تواصل مع فريق الدعم

---

## 🎓 أفضل الممارسات

1. **اكتب بيان واضح** لكل سند
2. **حدد المرجع** عند الإمكان (فاتورة/قيد)
3. **لا تحذف السندات** - استخدم الإلغاء بدلاً من الحذف
4. **راجع السندات دورياً** - للتدقيق المالي
5. **استخدم الفلاتر** - لإيجاد السندات بسرعة

---

## 📄 الترخيص

هذا النظام جزء من **Yasar Gold & Jewelry POS**  
جميع الحقوق محفوظة © 2025

---

**📱 استمتع باستخدام نظام السندات!**

**🚀 نظام احترافي لإدارة السندات المالية في محلات الذهب**