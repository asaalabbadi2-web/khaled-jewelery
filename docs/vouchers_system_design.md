# نظام السندات في ياسر للذهب
## تصميم وتنظيم سندات القبض والصرف

**التاريخ:** 10 أكتوبر 2025  
**الحالة:** 📝 تصميم وتخطيط

---

## 📊 أنواع السندات

### 1️⃣ **سند قبض (Receipt Voucher)**
- **الغرض:** توثيق استلام مبلغ نقدي أو ذهب
- **الحالات:**
  - تحصيل من عميل (سداد دين)
  - بيع نقدي فوري
  - استلام من مورد (في حالة مرتجع)
  - إيداع في الصندوق

### 2️⃣ **سند صرف (Payment Voucher)**
- **الغرض:** توثيق دفع مبلغ نقدي أو ذهب
- **الحالات:**
  - دفع لمورد (سداد دين)
  - شراء نقدي فوري
  - دفع لعميل (في حالة مرتجع)
  - سحب من الصندوق
  - مصروفات عامة

### 3️⃣ **سند تسوية (Adjustment Voucher)** - اختياري
- **الغرض:** تصحيح أخطاء أو تسويات
- **الحالات:**
  - فرق جرد
  - تصحيح أخطاء إدخال
  - تسوية أرصدة

---

## 🏗️ التصميم المعماري

### 📐 الهيكل المقترح:

```
┌─────────────────────────────────────────────────┐
│              📁 نظام السندات                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────┐  ┌──────────────┐           │
│  │ سند قبض      │  │ سند صرف      │           │
│  │ (Receipt)    │  │ (Payment)    │           │
│  └──────────────┘  └──────────────┘           │
│         │                  │                   │
│         └──────┬───────────┘                   │
│                ▼                               │
│        ┌──────────────┐                        │
│        │  Voucher     │                        │
│        │   Model      │                        │
│        └──────────────┘                        │
│                │                               │
│                ▼                               │
│     ┌─────────────────────┐                    │
│     │  القيد المحاسبي    │                    │
│     │ (Journal Entry)    │                    │
│     └─────────────────────┘                    │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 💾 قاعدة البيانات

### جدول `Voucher`:

```python
class Voucher(db.Model):
    """
    السندات - قبض وصرف
    """
    __tablename__ = 'voucher'
    
    id = db.Column(db.Integer, primary_key=True)
    
    # رقم السند (تسلسلي حسب النوع والسنة)
    voucher_number = db.Column(db.String(50), unique=True, nullable=False, index=True)
    # مثال: RV-2025-00001 (Receipt Voucher)
    #       PV-2025-00001 (Payment Voucher)
    
    # نوع السند
    voucher_type = db.Column(db.String(20), nullable=False)
    # القيم: 'receipt' (قبض), 'payment' (صرف), 'adjustment' (تسوية)
    
    # التاريخ
    date = db.Column(db.DateTime, nullable=False, default=db.func.now())
    
    # الطرف (عميل أو مورد)
    party_type = db.Column(db.String(20), nullable=True)  # 'customer', 'supplier', 'other'
    customer_id = db.Column(db.Integer, db.ForeignKey('customer.id'), nullable=True)
    supplier_id = db.Column(db.Integer, db.ForeignKey('supplier.id'), nullable=True)
    
    # اسم الطرف (إذا كان غير مسجل في النظام)
    party_name = db.Column(db.String(200), nullable=True)
    
    # المبلغ
    amount_cash = db.Column(db.Float, default=0.0)  # المبلغ النقدي
    amount_gold_weight = db.Column(db.Float, nullable=True)  # وزن الذهب
    amount_gold_karat = db.Column(db.Float, nullable=True)  # عيار الذهب
    
    # طريقة الدفع
    payment_method = db.Column(db.String(50), nullable=True)
    # 'cash' (نقدي), 'bank_transfer' (تحويل بنكي), 'check' (شيك), 'gold' (ذهب)
    
    # تفاصيل الشيك (إذا كانت الطريقة شيك)
    check_number = db.Column(db.String(50), nullable=True)
    check_date = db.Column(db.Date, nullable=True)
    bank_name = db.Column(db.String(100), nullable=True)
    
    # الحساب المقابل (من شجرة الحسابات)
    account_id = db.Column(db.Integer, db.ForeignKey('account.id'), nullable=True)
    
    # البيان/الملاحظات
    description = db.Column(db.Text, nullable=True)
    notes = db.Column(db.Text, nullable=True)
    
    # الفاتورة المرتبطة (إن وجدت)
    related_invoice_id = db.Column(db.Integer, db.ForeignKey('invoice.id'), nullable=True)
    
    # القيد المحاسبي المرتبط
    journal_entry_id = db.Column(db.Integer, db.ForeignKey('journal_entry.id'), nullable=True)
    
    # المستخدم الذي أنشأ السند
    created_by = db.Column(db.String(100), nullable=True)
    created_at = db.Column(db.DateTime, default=db.func.now())
    
    # الحذف الآمن
    is_deleted = db.Column(db.Boolean, default=False)
    deleted_at = db.Column(db.DateTime, nullable=True)
    deleted_by = db.Column(db.String(100), nullable=True)
    
    # العلاقات
    customer = db.relationship('Customer', foreign_keys=[customer_id], backref='vouchers')
    supplier = db.relationship('Supplier', foreign_keys=[supplier_id], backref='vouchers')
    account = db.relationship('Account', backref='vouchers')
    related_invoice = db.relationship('Invoice', backref='vouchers')
    journal_entry = db.relationship('JournalEntry', backref='voucher', uselist=False)
```

---

## 🔄 آلية العمل

### 📥 سند القبض (Receipt Voucher):

#### الحالة 1: تحصيل من عميل
```
المستخدم يدخل:
├── نوع السند: قبض
├── الطرف: عميل (اختيار من القائمة)
├── المبلغ: 1000 ريال
├── طريقة الدفع: نقدي
└── البيان: تحصيل من العميل أحمد

القيد المحاسبي التلقائي:
من حـ/ الصندوق (أو البنك)     1000    [مدين]
    إلى حـ/ العميل - أحمد      1000    [دائن]
```

#### الحالة 2: بيع ذهب نقدي (مباشر من السند)
```
المستخدم يدخل:
├── نوع السند: قبض
├── الطرف: عميل (أو زبون عابر)
├── المبلغ: 5000 ريال
├── ذهب: 10 جرام عيار 21
└── البيان: بيع ذهب نقدي

القيد المحاسبي:
من حـ/ الصندوق               5000    [مدين]
    إلى حـ/ المخزون - ذهب     (قيمة الذهب)  [دائن]
    إلى حـ/ الإيرادات          (الربح)       [دائن]
```

---

### 📤 سند الصرف (Payment Voucher):

#### الحالة 1: دفع لمورد
```
المستخدم يدخل:
├── نوع السند: صرف
├── الطرف: مورد (اختيار من القائمة)
├── المبلغ: 10000 ريال
├── طريقة الدفع: شيك
├── رقم الشيك: 123456
└── البيان: سداد للمورد محمد

القيد المحاسبي:
من حـ/ المورد - محمد          10000   [مدين]
    إلى حـ/ البنك             10000   [دائن]
```

#### الحالة 2: مصروفات عامة
```
المستخدم يدخل:
├── نوع السند: صرف
├── الطرف: أخرى
├── اسم الطرف: شركة الكهرباء
├── المبلغ: 500 ريال
├── الحساب: مصروفات الكهرباء
└── البيان: فاتورة كهرباء شهر أكتوبر

القيد المحاسبي:
من حـ/ مصروفات الكهرباء       500     [مدين]
    إلى حـ/ الصندوق           500     [دائن]
```

---

## 🎨 تصميم الواجهات (UI/UX)

### 📱 شاشة إنشاء سند قبض:

```
┌─────────────────────────────────────────┐
│  ➕ سند قبض جديد               [حفظ] │
├─────────────────────────────────────────┤
│                                         │
│  📅 التاريخ: [2025-10-10]              │
│  🔢 رقم السند: RV-2025-00123           │
│                                         │
│  👤 الطرف:                              │
│     ⚪ عميل  ⚪ مورد  ⚪ أخرى          │
│     [اختر عميل ▼]                      │
│                                         │
│  💰 المبلغ:                             │
│     💵 نقدي: [______] ريال             │
│     🏆 ذهب:  [______] جرام             │
│              عيار [21 ▼]               │
│                                         │
│  💳 طريقة الدفع:                       │
│     ⚪ نقدي  ⚪ بنكي  ⚪ شيك           │
│                                         │
│  📝 البيان:                             │
│     [____________________________]      │
│                                         │
│  📊 القيد المحاسبي (معاينة):           │
│  ┌─────────────────────────────┐       │
│  │ من حـ/ الصندوق    1000 [✓] │       │
│  │ إلى حـ/ العميل     1000 [✓] │       │
│  └─────────────────────────────┘       │
│                                         │
│       [حفظ وطباعة]    [حفظ]           │
└─────────────────────────────────────────┘
```

### 📊 شاشة قائمة السندات:

```
┌─────────────────────────────────────────────────┐
│  📋 السندات                           [+ جديد] │
├─────────────────────────────────────────────────┤
│                                                 │
│  🔍 [بحث...] 📅 [من-إلى] 🏷️ [النوع ▼]       │
│                                                 │
│  📊 الإحصائيات:                                │
│  ┌──────────┬──────────┬──────────┐            │
│  │ القبض    │ الصرف    │ الصافي   │            │
│  │ 50,000   │ 30,000   │ +20,000  │            │
│  └──────────┴──────────┴──────────┘            │
│                                                 │
│  📄 السندات:                                   │
│  ┌─────────────────────────────────────┐       │
│  │ 🟢 RV-2025-00123  │ قبض  │ 1,000   │       │
│  │ أحمد محمد         │ 10/10│ [👁️📝]  │       │
│  ├─────────────────────────────────────┤       │
│  │ 🔴 PV-2025-00045  │ صرف  │ 5,000   │       │
│  │ مورد الذهب        │ 09/10│ [👁️📝]  │       │
│  └─────────────────────────────────────┘       │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🔐 قواعد العمل (Business Rules)

### 1. **التسلسل الآلي للأرقام:**
```python
def generate_voucher_number(voucher_type, date):
    """
    توليد رقم سند تلقائي
    """
    year = date.year
    prefix = 'RV' if voucher_type == 'receipt' else 'PV'
    
    # البحث عن آخر رقم في نفس السنة والنوع
    last_voucher = Voucher.query.filter(
        Voucher.voucher_number.like(f'{prefix}-{year}-%')
    ).order_by(Voucher.id.desc()).first()
    
    if last_voucher:
        last_num = int(last_voucher.voucher_number.split('-')[-1])
        new_num = last_num + 1
    else:
        new_num = 1
    
    return f"{prefix}-{year}-{new_num:05d}"
```

### 2. **التحقق من الرصيد (للصرف):**
```python
def validate_payment_voucher(voucher):
    """
    التحقق من توفر الرصيد قبل الصرف
    """
    if voucher.payment_method == 'cash':
        # التحقق من رصيد الصندوق
        cash_balance = get_cash_balance()
        if cash_balance < voucher.amount_cash:
            raise ValueError("رصيد الصندوق غير كافٍ")
    
    elif voucher.payment_method == 'bank_transfer':
        # التحقق من رصيد البنك
        bank_balance = get_bank_balance()
        if bank_balance < voucher.amount_cash:
            raise ValueError("رصيد البنك غير كافٍ")
```

### 3. **إنشاء القيد المحاسبي تلقائياً:**
```python
def create_journal_entry_from_voucher(voucher):
    """
    إنشاء قيد محاسبي من السند
    """
    journal_entry = JournalEntry(
        date=voucher.date,
        description=voucher.description or f"سند {voucher.voucher_type} رقم {voucher.voucher_number}",
        reference_type='voucher',
        reference_id=voucher.id
    )
    
    if voucher.voucher_type == 'receipt':
        # سند قبض
        # مدين: الصندوق/البنك
        journal_entry.lines.append(JournalEntryLine(
            account_id=get_cash_or_bank_account_id(voucher.payment_method),
            debit_cash=voucher.amount_cash,
            credit_cash=0
        ))
        
        # دائن: العميل/المورد/الحساب المقابل
        journal_entry.lines.append(JournalEntryLine(
            account_id=get_counterpart_account_id(voucher),
            debit_cash=0,
            credit_cash=voucher.amount_cash
        ))
    
    elif voucher.voucher_type == 'payment':
        # سند صرف (عكس القبض)
        # ... نفس المنطق بالعكس
    
    db.session.add(journal_entry)
    voucher.journal_entry_id = journal_entry.id
```

---

## 📍 التنظيم في النظام

### 🏪 في قسم نقاط البيع (POS):
```
نقاط البيع
├── فواتير العملاء
│   ├── فاتورة بيع
│   ├── شراء كسر من عميل
│   ├── مرتجع بيع
│   └── مرتجع شراء كسر
│
└── السندات اليومية  ← 📌 هنا
    ├── سند قبض من عميل
    └── سند صرف لعميل
```

### 💼 في قسم المحاسبة (Accounting):
```
المحاسبة
├── المشتريات (الموردين)
│   ├── فاتورة شراء من مورد
│   └── مرتجع شراء
│
├── السندات  ← 📌 هنا
│   ├── سند قبض عام
│   ├── سند صرف عام
│   └── قائمة جميع السندات
│
├── القيود المحاسبية
└── التقارير
```

---

## 🎯 الميزات المقترحة

### 1. **الطباعة التلقائية:**
- قالب طباعة احترافي للسندات
- رمز QR للتحقق من السند
- ختم وتوقيع إلكتروني

### 2. **التنبيهات:**
- تنبيه عند انخفاض رصيد الصندوق
- تنبيه للشيكات المستحقة
- تذكير بالمدفوعات المؤجلة

### 3. **التكامل:**
- ربط السندات بالفواتير تلقائياً
- تحديث أرصدة العملاء والموردين فورياً
- إنشاء قيود محاسبية آلياً

### 4. **الأمان:**
- صلاحيات مختلفة لكل نوع سند
- سجل تدقيق (Audit Trail) لكل عملية
- منع تعديل السندات المعتمدة

---

## 📊 التقارير المطلوبة

### 1. **تقرير حركة الصندوق:**
```
التاريخ     | النوع  | البيان              | قبض    | صرف    | الرصيد
-----------+-------+--------------------+-------+-------+--------
10/10/2025 | افتتاحي| رصيد افتتاحي       |        |        | 10,000
10/10/2025 | قبض   | من العميل أحمد      | 1,000  |        | 11,000
10/10/2025 | صرف   | للمورد محمد         |        | 5,000  | 6,000
-----------+-------+--------------------+-------+-------+--------
الإجمالي:                              | 1,000  | 5,000  | 6,000
```

### 2. **تقرير الشيكات:**
- شيكات تحت التحصيل
- شيكات مستحقة الدفع
- شيكات مرتجعة

### 3. **تقرير المقبوضات والمدفوعات:**
- حسب العميل/المورد
- حسب طريقة الدفع
- حسب الفترة الزمنية

---

## ✅ خطة التنفيذ

### المرحلة 1: Backend
- [ ] إنشاء جدول `Voucher`
- [ ] دوال توليد الأرقام التلقائية
- [ ] دوال التحقق (Validation)
- [ ] ربط القيود المحاسبية
- [ ] API Endpoints

### المرحلة 2: Frontend
- [ ] شاشة إنشاء سند قبض
- [ ] شاشة إنشاء سند صرف
- [ ] قائمة السندات مع الفلاتر
- [ ] معاينة وطباعة السند

### المرحلة 3: التكامل
- [ ] ربط السندات بالفواتير
- [ ] تحديث الأرصدة تلقائياً
- [ ] التقارير

---

**النتيجة:** نظام سندات شامل ومتكامل مع المحاسبة! 🎉
