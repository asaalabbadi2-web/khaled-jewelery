# إعادة تهيئة النظام - System Reset

## نظرة عامة
ميزة إعادة تهيئة النظام توفر خيارات متعددة لحذف البيانات وإعادة النظام إلى حالة معينة بشكل آمن ومنظم.

## الخيارات المتاحة

### 1. حذف العمليات (transactions)
**الوصف**: حذف جميع العمليات المحاسبية والمالية فقط

**ما يتم حذفه:**
- ✅ القيود المحاسبية (Journal Entries) وسطورها
- ✅ الفواتير (Invoices) وعناصرها ومدفوعاتها
- ✅ السندات (Vouchers) وسطورها
- ✅ سجلات الرواتب (Payroll)
- ✅ سجلات الحضور (Attendance)

**ما يبقى:**
- ✔️ العملاء والموردين
- ✔️ شجرة الحسابات
- ✔️ المواد والأصناف
- ✔️ الإعدادات
- ✔️ أسعار الذهب

**الاستخدام**: مفيد عندما تريد البدء من جديد في تسجيل العمليات مع الاحتفاظ بالبيانات الأساسية.

> ℹ️ **ملاحظة مهمة:** بالإضافة إلى الحذف، يتم الآن إعادة ضبط أرصدة الحسابات، والعملاء، والموردين إلى الصفر للتأكد من تطابق البيانات المعروضة مع قاعدة البيانات الفارغة.

---

### 2. حذف العملاء والموردين (customers_suppliers)
**الوصف**: حذف بيانات العملاء والموردين فقط

**ما يتم حذفه:**
- ✅ جميع العملاء (Customers)
- ✅ جميع الموردين (Suppliers)

**ما يبقى:**
- ✔️ العمليات والقيود
- ✔️ شجرة الحسابات
- ✔️ المواد والأصناف
- ✔️ الإعدادات

**⚠️ تحذير**: قد تواجه مشاكل في العمليات المرتبطة بعملاء أو موردين محذوفين.

---

### 3. إعادة تعيين الإعدادات (settings)
**الوصف**: إرجاع جميع الإعدادات للقيم الافتراضية

**الإعدادات الافتراضية:**
```python
main_karat = 21
currency_symbol = 'ريال'
tax_rate = 0.0
tax_enabled = False
invoice_prefix = 'INV-'
decimal_places = 3
date_format = 'yyyy-MM-dd'
default_discount_rate = 0.0
allow_discount = True
show_company_logo = False
company_name = 'محل الياسر للذهب والمجوهرات'
company_address = ''
company_phone = ''
company_tax_number = ''
```

**ما يبقى:**
- ✔️ جميع البيانات الأخرى (عمليات، عملاء، حسابات، إلخ)

---

### 4. إعادة تهيئة كاملة (all)
**الوصف**: حذف **كل شيء** وإعادة النظام لحالته الأولى

**ما يتم حذفه:**
- ❌ **كل البيانات** في قاعدة البيانات
- ❌ العمليات والقيود
- ❌ الفواتير والسندات
- ❌ العملاء والموردين
- ❌ شجرة الحسابات
- ❌ المواد والأصناف
- ❌ أسعار الذهب
- ❌ الإعدادات
- ❌ كل شيء!

**⚠️ تحذير شديد**: هذا الإجراء **لا يمكن التراجع عنه أبداً**!

**بعد التنفيذ**: ستحتاج إلى:
1. إعادة تحميل شجرة الحسابات: `python backend/seed_accounts.py`
2. إعادة إدخال البيانات الأساسية
3. إعادة ضبط الإعدادات

---

## استخدام API

### الحصول على معلومات النظام
```bash
GET /api/system/reset/info
```

**Response:**
```json
{
  "status": "success",
  "data": {
    "transactions": {
      "journal_entries": 36,
      "journal_entry_lines": 159,
      "invoices": 32,
      "invoice_items": 38,
      "invoice_payments": 17,
      "vouchers": 5,
      "voucher_lines": 14,
      "payroll_entries": 8,
      "attendance_records": 120
    },
    "customers_suppliers": {
      "customers": 2,
      "suppliers": 1
    },
    "master_data": {
      "accounts": 95,
      "items": 6,
      "gold_prices": 3,
      "payment_methods": 5,
      "safe_boxes": 4,
      "employees": 7,
      "app_users": 4,
      "accounting_mappings": 18
    },
    "settings": {
      "has_settings": true
    }
  }
}
```

---

### إعادة التهيئة
```bash
POST /api/system/reset
Content-Type: application/json

{
  "reset_type": "transactions"  // أو customers_suppliers أو settings أو all
}
```

**Response (Success):**
```json
{
  "status": "success",
  "message": "تم حذف جميع العمليات بنجاح (القيود، الفواتير، السندات)",
  "reset_type": "transactions"
}
```

**Response (Error):**
```json
{
  "status": "error",
  "message": "نوع إعادة التهيئة غير صحيح: xyz..."
}
```

---

## استخدام الواجهة (Flutter)

### الوصول
1. افتح التطبيق
2. اضغط على **⚙️ (أيقونة الإعدادات)**
3. اختر **"إعادة تهيئة النظام"** (باللون الأحمر)

### الشاشة
- **معلومات النظام**: عرض عدد السجلات في كل قسم
- **خيارات إعادة التهيئة**: 4 بطاقات ملونة لكل خيار
- **تأكيد مزدوج**: حوار تأكيد قبل الحذف
- **تحذيرات**: رسائل واضحة عن عدم إمكانية التراجع

### الأمان
- ✅ تأكيد مزدوج قبل أي حذف
- ✅ رسائل تحذير واضحة
- ✅ عرض تفصيلي لما سيتم حذفه
- ✅ اللون الأحمر للعمليات الخطرة
- ✅ تعطيل الأزرار إذا لم تكن هناك بيانات

---

## الكود

### Backend
- **routes.py**: 
  - `system_reset()` - Endpoint رئيسي
  - `_reset_transactions()` - حذف العمليات
  - `_reset_customers_suppliers()` - حذف العملاء/الموردين
  - `_reset_settings()` - إعادة تعيين الإعدادات
  - `get_reset_info()` - معلومات النظام

### Frontend
- **api_service.dart**:
  - `resetSystem({resetType})` - استدعاء API
  - `getSystemResetInfo()` - الحصول على المعلومات

- **system_reset_screen.dart**:
  - واجهة مستخدم كاملة
  - حوارات التأكيد
  - عرض الإحصائيات

- **home_screen.dart**:
  - إضافة رابط في القائمة

---

## أمثلة الاستخدام

### مثال 1: حذف العمليات فقط
```dart
await _apiService.resetSystem(resetType: 'transactions');
```

### مثال 2: إعادة تعيين الإعدادات
```dart
await _apiService.resetSystem(resetType: 'settings');
```

### مثال 3: إعادة تهيئة كاملة
```dart
await _apiService.resetSystem(resetType: 'all');
// بعدها: تشغيل python backend/seed_accounts.py
```

---

## الحالات الشائعة

### 🔄 بدء سنة مالية جديدة
**الحل**: `transactions` - يحذف العمليات ويحتفظ بالبيانات الأساسية

### 🔄 تجربة النظام
**الحل**: `all` - ثم إعادة تحميل الحسابات

### 🔄 تنظيف قاعدة البيانات
**الحل**: `transactions` - للاحتفاظ بالعملاء والموردين

### 🔄 مشكلة في الإعدادات
**الحل**: `settings` - إرجاع الإعدادات للوضع الافتراضي

---

## تحذيرات مهمة

⚠️ **جميع عمليات الحذف نهائية ولا يمكن التراجع عنها**

⚠️ **خذ نسخة احتياطية قبل أي عملية حذف**

⚠️ **تأكد من فهم ما ستحذفه بالضبط**

⚠️ **بعد "إعادة تهيئة كاملة" ستحتاج إلى إعادة تحميل شجرة الحسابات**

---

## النسخ الاحتياطي

قبل إعادة التهيئة، يُنصح بأخذ نسخة احتياطية:

```bash
# نسخ قاعدة البيانات
cp backend/app.db backend/app.db.backup_$(date +%Y%m%d_%H%M%S)

# أو استخدام SQLite dump
sqlite3 backend/app.db .dump > backup_$(date +%Y%m%d_%H%M%S).sql
```

---

## الملفات المعدلة
- `backend/routes.py` - إضافة endpoints وdالات مساعدة
- `frontend/lib/api_service.dart` - إضافة طرق API
- `frontend/lib/screens/system_reset_screen.dart` - شاشة جديدة
- `frontend/lib/screens/home_screen.dart` - إضافة رابط
- `docs/system_reset.md` - هذا الملف

---

**تاريخ الإنشاء**: 16 أكتوبر 2025
**الإصدار**: 1.0
**الحالة**: ✅ جاهز للاستخدام
