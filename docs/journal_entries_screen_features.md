# شاشة قيود اليومية الاحترافية
## Journal Entries Professional Screen

### 📋 المزايا الرئيسية / Main Features

#### 1️⃣ **البحث المتقدم / Advanced Search**
- بحث فوري في الوصف، التاريخ، أو رقم القيد
- واجهة بحث مدمجة في شريط التطبيق
- إلغاء البحث بسهولة

#### 2️⃣ **الفلاتر المتقدمة / Advanced Filters**
- **فلتر التاريخ**: اختيار نطاق زمني محدد
- **فلتر الحساب**: عرض القيود التي تحتوي على حساب معين
- **فلتر المبلغ**: تحديد حد أدنى وأعلى للمبالغ النقدية
- عرض الفلاتر النشطة كـ Chips قابلة للإزالة
- زر "مسح الكل" لإزالة جميع الفلاتر دفعة واحدة

#### 3️⃣ **الترتيب المتعدد / Multiple Sorting**
- ترتيب حسب **التاريخ** (الأحدث/الأقدم أولاً)
- ترتيب حسب **المبلغ** (الأعلى/الأقل أولاً)
- ترتيب حسب **رقم القيد** (تصاعدي/تنازلي)
- واجهة حوارية بسيطة للاختيار

#### 4️⃣ **الإحصائيات الشاملة / Comprehensive Statistics**
- بطاقة إحصائيات أعلى الشاشة تعرض:
  - **عدد القيود**: إجمالي القيود المعروضة (بعد التصفية)
  - **إجمالي النقد**: مجموع المبالغ النقدية المدينة
  - **إجمالي الذهب**: مجموع الذهب المحول للعيار الرئيسي (21)
- تحديث فوري عند التصفية أو البحث

#### 5️⃣ **التصميم الاحترافي / Professional Design**
- **الثيم الذهبي/الأسود**: يتماشى مع هوية التطبيق
- **بطاقات احترافية**: 
  - شارة رقم القيد بتصميم ذهبي
  - عرض التاريخ بأيقونة
  - المبالغ ملونة (أخضر للنقد، كهرماني للذهب)
  - حدود ذهبية شفافة للبطاقات
- **الحذف بالسحب**: اسحب البطاقة لليسار للحذف مع تأكيد
- **رسائل تأكيد احترافية**: حوارات مصممة بالثيم الداكن

#### 6️⃣ **التفاعلية والأداء / Interactivity & Performance**
- **Pull to Refresh**: اسحب للأسفل لتحديث البيانات
- **حالة فارغة جميلة**: رسالة وأيقونة عند عدم وجود قيود
- **حالة "لا نتائج"**: رسالة مخصصة عند البحث بدون نتائج
- **تحميل سريع**: جلب متوازي للبيانات (Settings + Entries + Accounts)

#### 7️⃣ **التنقل السلس / Smooth Navigation**
- الضغط على أي قيد للتعديل
- زر "إضافة قيد" في شريط التطبيق
- العودة التلقائية وتحديث البيانات بعد الإضافة/التعديل

#### 8️⃣ **دعم اللغتين / Bilingual Support**
- واجهة عربية كاملة (افتراضي)
- دعم الإنجليزية
- جميع النصوص والرسائل مترجمة

---

### 🎨 **عناصر التصميم / Design Elements**

#### الألوان / Colors
```dart
gold: Color(0xFFF7C873)        // الذهبي - للعناصر المميزة
darkBg: Color(0xFF232323)      // خلفية داكنة
cardBg: Color(0xFF2D2D2D)      // خلفية البطاقات
green: Colors.green[400]       // للمبالغ النقدية
amber: Colors.amber[600]       // للذهب
red: Colors.red.shade700       // للحذف
```

#### الأيقونات / Icons
- `receipt_long` - قائمة القيود
- `search` - البحث
- `filter_list` - الفلاتر
- `sort` - الترتيب
- `add` - إضافة قيد
- `monetization_on` - النقد
- `balance` - الذهب
- `calendar_today` - التاريخ
- `delete` - الحذف

---

### 📱 **كيفية الاستخدام / How to Use**

#### الوصول للشاشة
```dart
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => JournalEntriesListScreen(isArabic: true),
  ),
);
```

#### من الشاشة الرئيسية
```
الرئيسية → المحاسبة → قيود اليومية
```

---

### 🔧 **المتطلبات التقنية / Technical Requirements**

#### الحزم المطلوبة / Required Packages
```yaml
dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0  # للتنسيقات والتواريخ
```

#### API Endpoints المستخدمة
```
GET /api/settings           - جلب الإعدادات (main_karat)
GET /api/journal-entries    - جلب جميع القيود
GET /api/accounts           - جلب الحسابات للفلتر
DELETE /api/journal-entries/<id> - حذف قيد
```

---

### 🎯 **حالات الاستخدام / Use Cases**

#### 1. المحاسب اليومي
- عرض قيود اليوم الحالي فقط (فلتر التاريخ)
- ترتيب حسب رقم القيد
- إحصائيات سريعة للمجاميع

#### 2. المراجعة الشهرية
- فلتر نطاق تاريخ شهر كامل
- ترتيب حسب المبلغ (الأعلى أولاً)
- تحديد القيود الكبيرة

#### 3. تدقيق حساب معين
- فلتر حسب رقم الحساب
- عرض جميع القيود المرتبطة
- البحث في الأوصاف

#### 4. البحث السريع
- كتابة رقم القيد أو كلمة من الوصف
- نتائج فورية

---

### ⚙️ **الإعدادات القابلة للتخصيص / Customizable Settings**

يمكن تعديل الإعدادات التالية في الكود:

```dart
// في _JournalEntriesListScreenState

// الترتيب الافتراضي
String _sortBy = 'date_desc';  // يمكن تغييره لـ 'id_desc', 'amount_desc', إلخ

// العيار الرئيسي (يتم جلبه من API)
int _mainKarat = 21;  // افتراضي، يتم تحديثه من الإعدادات
```

---

### 🐛 **التعامل مع الأخطاء / Error Handling**

الشاشة تتعامل مع:
- ✅ فشل تحميل البيانات (رسالة خطأ)
- ✅ فشل الحذف (رسالة خطأ + عدم إزالة القيد)
- ✅ قيود فارغة (رسالة ودية)
- ✅ بحث بدون نتائج (رسالة مخصصة)
- ✅ تنسيقات تاريخ غير صحيحة

---

### 📊 **الأداء / Performance**

- **جلب متوازي**: `Future.wait()` لتحميل البيانات دفعة واحدة
- **فلترة محلية**: التصفية تحدث في الذاكرة (لا طلبات إضافية للـ API)
- **Lazy Loading**: القوائم الطويلة تُحمّل بكفاءة مع `ListView.builder`
- **setState ذكي**: تحديث فقط عند الحاجة

---

### 🔐 **الأمان / Security**

- ✅ تأكيد قبل الحذف (منع الحذف العرضي)
- ✅ التحقق من mounted قبل setState (منع memory leaks)
- ✅ Try-Catch في جميع عمليات API

---

### 🚀 **التحسينات المستقبلية / Future Enhancements**

#### مقترحة للإصدارات القادمة:
1. **تصدير PDF**: تصدير القيود المعروضة كملف PDF
2. **تصدير Excel/CSV**: لتحليل البيانات في برامج خارجية
3. **الطباعة**: طباعة مباشرة للقيود
4. **التجميع**: عرض قيود مجمعة حسب التاريخ أو الحساب
5. **الرسوم البيانية**: charts لإجمالي القيود الشهرية
6. **الفلتر المتقدم**: 
   - فلتر حسب نوع المعاملة (نقد/ذهب/كليهما)
   - فلتر حسب المستخدم (إذا تم إضافة تتبع المستخدمين)
7. **التحميل التدريجي**: Pagination للقوائم الطويلة جداً
8. **المفضلة**: وضع علامة على قيود مهمة
9. **الملاحظات**: إضافة تعليقات على القيود

---

### 📝 **الملاحظات التطويرية / Developer Notes**

#### البنية
```
JournalEntriesListScreen (StatefulWidget)
  └─ _JournalEntriesListScreenState
      ├─ _applyFilters()       - منطق التصفية
      ├─ _sortEntries()         - منطق الترتيب
      ├─ _calculateStatistics() - حساب الإحصائيات
      ├─ _showFilterDialog()    - عرض حوار الفلاتر
      ├─ _showSortDialog()      - عرض حوار الترتيب
      └─ build()                - بناء الواجهة

_FilterDialog (StatefulWidget)
  └─ حوار منفصل لإدارة الفلاتر
```

#### الحالة / State Management
```dart
// البيانات
List<dynamic> _allEntries      // جميع القيود (قبل الفلترة)
List<dynamic> _filteredEntries // القيود بعد الفلترة
List<dynamic> _accounts        // قائمة الحسابات

// الفلاتر
DateTimeRange? _dateRange
int? _selectedAccountId
double? _minAmount
double? _maxAmount
String _sortBy

// الإحصائيات
double _totalCash
double _totalGold
int _totalEntries
```

---

### 🎓 **أمثلة الكود / Code Examples**

#### استخدام الفلاتر برمجياً
```dart
// فلتر آخر 7 أيام
setState(() {
  _dateRange = DateTimeRange(
    start: DateTime.now().subtract(Duration(days: 7)),
    end: DateTime.now(),
  );
});
_applyFilters();

// فلتر حسب حساب معين
setState(() {
  _selectedAccountId = 123; // ID الحساب
});
_applyFilters();

// فلتر المبالغ الكبيرة فقط
setState(() {
  _minAmount = 10000.0;
});
_applyFilters();
```

---

### ✅ **التوافقية / Compatibility**

- ✅ iOS
- ✅ Android  
- ✅ Web
- ✅ Desktop (macOS, Windows, Linux)
- ✅ RTL Support (العربية)
- ✅ LTR Support (English)

---

### 📞 **الدعم / Support**

للأسئلة أو المشاكل، يرجى التواصل مع فريق التطوير.

**تاريخ الإنشاء**: 10 أكتوبر 2025  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للإنتاج
