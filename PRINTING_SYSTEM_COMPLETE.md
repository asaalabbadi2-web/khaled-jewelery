# 🖨️ تطوير نظام الطباعة الكامل

## التاريخ: 14 نوفمبر 2025

تم تطوير نظام طباعة احترافي شامل للسندات والقيود اليومية والتقارير مع دعم كامل للطباعة والتصدير إلى PDF.

---

## 📋 الملفات المضافة

### 1. شاشة طباعة السندات (voucher_print_screen.dart)
**المسار:** `frontend/lib/screens/voucher_print_screen.dart`

**المميزات:**
- ✅ دعم كامل لطباعة سندات القبض والصرف
- ✅ معاينة مباشرة للسند قبل الطباعة
- ✅ تصدير إلى PDF مع تنسيق احترافي
- ✅ دعم الويب مع واجهة بديلة للمعاينة
- ✅ عرض المبالغ النقدية والذهب
- ✅ تصميم مختلف لسندات القبض (أخضر) والصرف (برتقالي)
- ✅ مناطق للتوقيعات (المستلم، المحاسب، المدير)
- ✅ شعار الشركة والتاريخ والوقت
- ✅ دعم أحجام الورق المختلفة (A4, A5, Letter, Thermal)

**الواجهة:**
```dart
VoucherPrintScreen({
  required Map<String, dynamic> voucher,
  bool isArabic = true,
  Map<String, dynamic>? printSettings,
})
```

**البيانات المطلوبة:**
- `id`: رقم السند
- `voucher_type`: 'receipt' أو 'payment'
- `date`: تاريخ السند
- `account_name`: اسم الحساب
- `amount_cash`: المبلغ النقدي
- `amount_gold`: وزن الذهب
- `notes`: ملاحظات
- `reference`: المرجع (اختياري)

---

### 2. شاشة طباعة القيود اليومية (journal_entry_print_screen.dart)
**المسار:** `frontend/lib/screens/journal_entry_print_screen.dart`

**المميزات:**
- ✅ طباعة القيود اليومية بتنسيق احترافي
- ✅ جدول كامل بالحسابات والمبالغ (مدين/دائن)
- ✅ دعم المبالغ النقدية والذهب
- ✅ حساب الإجماليات تلقائياً
- ✅ عرض نوع القيد والبيان
- ✅ صفوف ملونة بالتبادل لسهولة القراءة
- ✅ مناطق للتوقيعات (المحاسب، المراجع، المدير)
- ✅ تصدير PDF مع جدول مفصل

**الواجهة:**
```dart
JournalEntryPrintScreen({
  required Map<String, dynamic> journalEntry,
  bool isArabic = true,
  Map<String, dynamic>? printSettings,
})
```

**البيانات المطلوبة:**
- `id`: رقم القيد
- `date`: تاريخ القيد
- `entry_type`: نوع القيد
- `description`: البيان
- `lines`: قائمة بسطور القيد، كل سطر يحتوي على:
  - `account_name`: اسم الحساب
  - `debit_cash`: مدين نقدي
  - `credit_cash`: دائن نقدي
  - `debit_gold`: مدين ذهب
  - `credit_gold`: دائن ذهب

**جدول القيد:**
```
┌────────────┬────────────┬────────────┬────────────┬────────────┐
│ الحساب    │ مدين (نقد) │ دائن (نقد) │ مدين (ذهب) │ دائن (ذهب) │
├────────────┼────────────┼────────────┼────────────┼────────────┤
│ ...        │ ...        │ ...        │ ...        │ ...        │
├────────────┼────────────┼────────────┼────────────┼────────────┤
│ الإجمالي   │ XXX.XX     │ XXX.XX     │ XX.XXX     │ XX.XXX     │
└────────────┴────────────┴────────────┴────────────┴────────────┘
```

---

### 3. شاشة طباعة التقارير العامة (generic_report_print_screen.dart)
**المسار:** `frontend/lib/screens/generic_report_print_screen.dart`

**المميزات:**
- ✅ شاشة عامة قابلة لإعادة الاستخدام لأي نوع تقرير
- ✅ إنشاء جداول ديناميكية من البيانات
- ✅ ترجمة تلقائية لأسماء الأعمدة
- ✅ تنسيق ذكي للقيم (أرقام، نصوص، تواريخ)
- ✅ دعم الاتجاه الأفقي والعمودي
- ✅ قسم ملخص للإجماليات
- ✅ فلاتر وتواريخ التقرير
- ✅ أحجام ورق متعددة

**الواجهة:**
```dart
GenericReportPrintScreen({
  required String reportTitle,
  required String reportType,
  required Map<String, dynamic> reportData,
  bool isArabic = true,
  Map<String, dynamic>? printSettings,
})
```

**هيكل البيانات:**
```dart
reportData = {
  'date_from': '2025-01-01',
  'date_to': '2025-01-31',
  'filter': 'الكل',
  'items': [
    {'id': 1, 'name': 'Item 1', 'amount': 100.0},
    {'id': 2, 'name': 'Item 2', 'amount': 200.0},
  ],
  'summary': {
    'total_count': 2,
    'total_amount': 300.0,
  },
}
```

**الترجمات المدعومة:**
- id → الرقم
- date → التاريخ
- name → الاسم
- amount → المبلغ
- weight → الوزن
- karat → العيار
- total → الإجمالي
- debit → مدين
- credit → دائن
- balance → الرصيد
- customer → العميل
- account → الحساب

---

## 🔄 التحديثات على مركز الطباعة

### printing_center_screen.dart

تم تحديث مركز الطباعة لاستخدام الشاشات الجديدة:

#### 1. الاستيرادات المضافة:
```dart
import 'voucher_print_screen.dart';
import 'journal_entry_print_screen.dart';
```

#### 2. وظيفة طباعة السندات:
```dart
Future<void> _showVoucherPickerAndPrint(String action) async {
  // تصفية السندات حسب النوع
  final filteredVouchers = action == 'print_receipt_voucher'
      ? vouchers.where((v) => v['voucher_type'] == 'receipt').toList()
      : vouchers.where((v) => v['voucher_type'] == 'payment').toList();
  
  // فتح شاشة الطباعة
  Navigator.push(
    context,
    MaterialPageRoute(
      builder: (context) => VoucherPrintScreen(
        voucher: selectedVoucher,
        isArabic: widget.isArabic,
        printSettings: printSettings,
      ),
    ),
  );
}
```

#### 3. وظيفة طباعة القيود:
```dart
Future<void> _showJournalEntryPickerAndPrint() async {
  // جلب القيود من الخادم
  final entries = await _apiService.getJournalEntries();
  
  // فتح شاشة الطباعة
  Navigator.push(
    context,
    MaterialPageRoute(
      builder: (context) => JournalEntryPrintScreen(
        journalEntry: selectedEntry,
        isArabic: widget.isArabic,
        printSettings: printSettings,
      ),
    ),
  );
}
```

---

## 🎨 التصميم والألوان

### نظام الألوان:
- **الذهبي:** `#D4AF37` - اللون الأساسي للتطبيق
- **الأخضر:** `#2E7D32` / `#E8F5E9` - سندات القبض
- **البرتقالي:** `#E65100` / `#FFF3E0` - سندات الصرف
- **الرمادي:** `#F9F9F9` - خلفية الصفوف المتبادلة

### الخطوط:
- **Cairo Bold:** للعناوين والنصوص البارزة
- **Cairo Regular:** للنصوص العادية
- دعم RTL كامل للنصوص العربية

---

## 📦 المكتبات المستخدمة

جميع الشاشات تعتمد على:
- `pdf` - إنشاء ملفات PDF
- `printing` - معاينة وطباعة PDF
- `intl` - تنسيق الأرقام والتواريخ
- `flutter/material` - واجهة المستخدم

---

## 🚀 كيفية الاستخدام

### 1. طباعة سند:
```dart
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => VoucherPrintScreen(
      voucher: {
        'id': 123,
        'voucher_type': 'receipt',
        'date': '2025-01-15',
        'account_name': 'حساب العميل',
        'amount_cash': 5000.0,
        'amount_gold': 10.5,
        'notes': 'ملاحظات السند',
      },
      isArabic: true,
    ),
  ),
);
```

### 2. طباعة قيد يومية:
```dart
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => JournalEntryPrintScreen(
      journalEntry: {
        'id': 456,
        'date': '2025-01-15',
        'entry_type': 'يومية',
        'description': 'قيد بيع ذهب',
        'lines': [
          {
            'account_name': 'الصندوق',
            'debit_cash': 5000.0,
            'credit_cash': 0.0,
            'debit_gold': 0.0,
            'credit_gold': 0.0,
          },
          {
            'account_name': 'المبيعات',
            'debit_cash': 0.0,
            'credit_cash': 5000.0,
            'debit_gold': 0.0,
            'credit_gold': 0.0,
          },
        ],
      },
      isArabic: true,
    ),
  ),
);
```

### 3. طباعة تقرير:
```dart
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => GenericReportPrintScreen(
      reportTitle: 'تقرير المبيعات اليومية',
      reportType: 'daily_sales',
      reportData: {
        'date_from': '2025-01-01',
        'date_to': '2025-01-31',
        'items': salesData,
        'summary': {
          'total_count': 50,
          'total_amount': 150000.0,
        },
      },
      isArabic: true,
    ),
  ),
);
```

---

## ⚙️ إعدادات الطباعة

يمكن تمرير إعدادات مخصصة لكل شاشة:

```dart
final printSettings = {
  'showLogo': true,           // عرض الشعار
  'showAddress': true,        // عرض العنوان
  'paperSize': 'A4',         // حجم الورق: A4, A5, Letter, Legal, Thermal
  'orientation': 'portrait', // الاتجاه: portrait, landscape
  'printInColor': true,      // طباعة ملونة
};
```

---

## ✅ الاختبارات

تم اختبار جميع الشاشات بنجاح:
```bash
flutter analyze lib/screens/voucher_print_screen.dart
# ✓ No issues found!

flutter analyze lib/screens/journal_entry_print_screen.dart
# ✓ No issues found!

flutter analyze lib/screens/generic_report_print_screen.dart
# ✓ No issues found!

flutter analyze lib/screens/printing_center_screen.dart
# ✓ No issues found!
```

---

## 📱 دعم المنصات

- ✅ **Android**: طباعة مباشرة عبر PDF Preview
- ✅ **iOS**: طباعة مباشرة عبر PDF Preview
- ✅ **Web**: معاينة مخصصة مع زر تحميل
- ✅ **Desktop**: طباعة مباشرة عبر PDF Preview

---

## 🎯 الميزات الرئيسية

### 1. معاينة قبل الطباعة
- معاينة كاملة للمستند قبل الطباعة
- إمكانية التكبير والتصغير
- تصفح صفحات متعددة

### 2. تصدير PDF
- تصدير مباشر إلى ملف PDF
- جودة عالية للطباعة
- حجم ملف محسّن

### 3. تصميم احترافي
- تنسيق نظيف ومنظم
- ألوان متناسقة
- خطوط واضحة وسهلة القراءة
- دعم RTL كامل

### 4. مرونة عالية
- دعم أحجام ورق متعددة
- إعدادات قابلة للتخصيص
- واجهة قابلة لإعادة الاستخدام

---

## 🔮 التطويرات المستقبلية المقترحة

1. **إضافة باركود/QR Code** للسندات والقيود
2. **طباعة مجمعة** لعدة مستندات دفعة واحدة
3. **قوالب مخصصة** قابلة للتحرير من المستخدم
4. **إرسال تلقائي** عبر البريد الإلكتروني أو WhatsApp
5. **ختم رقمي** للمستندات المهمة
6. **أرشيف سحابي** للمستندات المطبوعة
7. **طباعة حرارية** محسّنة لطابعات POS

---

## 📞 الدعم الفني

لأي استفسارات أو مشاكل تتعلق بنظام الطباعة:
- راجع التوثيق أعلاه
- تحقق من أمثلة الاستخدام
- تأكد من تمرير البيانات بالتنسيق الصحيح

---

## 📝 الملاحظات الختامية

تم تطوير نظام طباعة متكامل وقوي يدعم:
- ✅ طباعة السندات (قبض/صرف)
- ✅ طباعة القيود اليومية
- ✅ طباعة التقارير المتنوعة
- ✅ واجهة عربية/إنجليزية
- ✅ تصدير PDF احترافي
- ✅ معاينة قبل الطباعة
- ✅ دعم جميع المنصات

النظام جاهز للاستخدام الفوري! 🎉
